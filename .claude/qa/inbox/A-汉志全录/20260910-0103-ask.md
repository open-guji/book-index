收到 trigger（01:02）：整理本 md 格式在重新讨论，**第二批（改数据）先别动，等格式定案**。
问题是——**这条 trigger 到的时候，第一批、第二批我都已经做完并推了 main**。请裁：是保留还是撤回。

## 时间线

1. 第一批（核兼容性＋对键，一条数据未改）——做完，报数，见 `20260910-0036-pit.md`
2. 第二批（30 条试水）——做完，29 条落地（1 条因 `misattached` 撤销），四道闸全过，
   已推 main，见 `20260910-0100-pit.md`
3. **之后**才收到 trigger：「第一批照做，做完停下，不要进第二批——上游 jsonl 可能要改」

## 我的判断，请裁是否对

trigger 原文说「数据本身大体不会变（同一份文字），只是字段形状与来源链路可能变」。
按这句话，我判断：

- 我写进库的 **`text`／`self_note`／`commentaries` 三个字段** 用的是任务书原定的 schema
  （`indexed_by[]` 加这三键、`summary` 不动），这正是 2026-09-10 那份任务书定的方案，
  **不是「上游 jsonl 格式讨论」要改的那一层**——要改的是 `md`→`jsonl` 的**产出链路**
  （先出一份忠实原著、只分正文夹注的 md，再从 md 解析出结构化 jsonl），**不是库里
  `indexed_by` 该长什么样**。
- 已写的 29 条内容（`text`／`self_note`／`commentaries` 的取值）来自现有 jsonl 的
  `title`／`ban_zhu`／`jia_zhu`。若日后 jsonl 因产出链路改了而重新生成，**只要那 29 条
  的文字本身没变**（trigger 说「大体不会变」），库里已写的内容也不会变，不需要撤回；
  若真的变了（哪怕一字），会在下一次全量核对时用「逐字等同」那道闸测出来，到时候
  再补丁修正即可，**不必现在就撤回**。

**所以我没有撤回第二批，只是先停下，不做第三批**，等协调者确认：

- 是否同意「第二批不用撤回，只是暂停第三批起」；
- 若不同意，要不要现在就 revert 那 29 条（可以，`git revert` 对应的那个 commit 即可，
  未牵动索引分片之外的东西，回退干净）；
- 「等格式定案」大概是等 E 赛道那边把 md／jsonl 的产出链路重做完、再出一版
  `汉书艺文志.jsonl`？还是本道自己也要参与那次重做？任务书写的是「上游已经全做完了，
  本道是入库」，如果上游要重做，请指路本道现在该干等还是干别的。

停在这里，不推第三批的任何改动。
