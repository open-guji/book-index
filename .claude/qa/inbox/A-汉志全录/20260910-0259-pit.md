自查自纠一处：写批次时漏了协调者裁决的一半——「入库时剥掉①-⑳」只在**对键**时
剥了，**没在实际写入的 `text`／`self_note`／`commentaries` 里剥**。

协调者裁的原话（`12-对键判据四缺口.md` §三）：「`①` 是整理本自身的校記锚点……
入库时剥掉即可」。我对键的四层判据里确实剥了（`ANCHOR_RE = re.compile(r'[①-⑳]')`），
但那只用来算 key、决定「配不配」，**没把这条规则延伸到写入的字符串本身**——
所以 576 条里有 5 条把 jsonl 原样带着的锚点字符抄进库了：
`世本十五篇`／`馮商所續太史公七篇`／`杜文公五篇`／`孫軫五篇`／`泰階六符一卷`。

已补丁修复（剥掉锚点，逐字等同闸按「两边都剥」的口径重核，5/5 过），已推 main。
写在这里是想留一条经验：**判据要么写成函数两处都调，要么写完就去查另一处有没有
漏用同一条**——这次是同一条规则，两处代码，一处用了一处没用，正是坑 77 那型的
一个新变种（不是「同一件事两处实作」，是「同一件事一处实作、另一处忘了调用」）。

第四批（`xu`／`lei_total`／`lue_total`／`jiaoji` 归属方案）另写 `ask`。
