第二批（小批试水30条）报数，并报一处对键要修正的坑。

## 一、结果：29 条落地，1 条撤销

选法：诸家注最多 20 条 ＋ 自注他注并存 10 条（`議奏四十二篇` 那型），共 30 条。
其中 `周官經六篇`（`d59f1ioc6m0w`《周禮》）**撤销未写**，理由见下。
其余 29 条已写入 `text`／`self_note`／`commentaries` 三个新字段，`summary` 一字未改。

四道闸（对应任务书 §六 1–4）：

1. JSON 合法：29/29
2. 越界检：本批改动的键只有 `text`／`self_note`／`commentaries`／嵌套的 `by`，
   没有出现 `summary` 或别的键
3. 逐字等同：29/29，差 0（`text＋self_note＋commentaries[].text` 拼起来
   与 jsonl 该条 `title＋ban_zhu＋jia_zhu[].text` 拼起来逐字相同）
4. 抽样人工核：抽 20/29，`self_note` 里没有混进「某曰」引导语，
   `commentaries[].by` 与 jsonl 一致，未见归属错乱

## 二、坑：**对键时要把 `misattached: true` 的库记录排除掉**（第一批的对键结果要修正）

`周官經六篇` 按题＋篇卷数對到库中 `d59f1ioc6m0w`（题作《周禮》）的一条
`indexed_by[source=漢書藝文志]`，但那一条自己就带着

```json
"misattached": true,
"misattached_note": "2026-08-21 catalog_bound 覆驗：本節出《漢書藝文志》
（上限 qin-han），而本條為 ming 之作、撰人闕，著錄語與之全不相干——
係同題異書誤併。所指何書，僅憑題名卷數無從斷，未別立 Work，
記於 known-issues/著錄錯掛待建.json 待考。"
```

——**这条库记录早就被判定是「同题异书误併」，其实是明代的作品，不是《漢志》真正
著录的〈周官經〉**。差一点把《漢志》的班固自注、顏師古注写进一个明代书的条目里。

已撤销该条写入（`git checkout` 还原了那一个文件），**没有留任何改动**。

**全库排查**：`indexed_by[source=漢書藝文志]` 的 659 条里，`misattached:true` 的
一共 **2 条**——除了 `周官經六篇`（`d59f1ioc6m0w`）外，另一条是
`山海經十三篇`（`d59f1iky4tmo`）。这两条**从今往后的对键与写入一律排除**，
第一批报告里「对键清单」当时没做这层过滤，现补一句：
`周官經六篇`／`山海經十三篇`即使按题＋篇卷数對上了库中一条，**那条库记录不是
真正的漢志著录，等同于「整理本有、库无有效对应」**，写入时按「库无」处理，不写。

第三批起会先加这道过滤再选条目，不会重犯。

接着做第三批。
