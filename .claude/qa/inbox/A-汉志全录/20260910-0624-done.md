收工。用户要求所有云端道今天结束，按收工指令完成三件，**不用叫醒协调者**。

## 已保住的进度

- 四批任务书**全部收工**：576 条已全部写入 main（29 试水＋100×4＋147 收尾），
  四道闸每批全过；补丁修复了 5 条锚点漏剥；第四批（`xu`等 108 个非条目元素归属）
  方案已出，等协调者裁；4 组 5 条真正待核（`傳四十一篇`等）等协调者裁。
- GLM 擂台：11 条形近异文只跑通 4 条（接口在 `max_tokens` 某区间不稳，非模型问题），
  已如实记入 `调度/GLM外援怎么用.md` §十一·4 表 ＋ §十二整节，**已推 main**
  （推的时候撞了一次并发合并冲突——`A-考补卷4` 道同时也在加擂台表的行，
  两边的新增行都保住了，无丢数据）。
- 任务书 `进度/A-目录录入/任务书-A-汉志全录.md` 末尾已补「收工时状态」一节，
  交底了：四批各做到哪、下一步接手该干什么、GLM 擂台细节、以及协调者点名要问的
  「上游 jsonl 重出一版时已入库内容怎么核」——本道给的答案是「先 diff 新旧两版
  jsonl 找出真的变了哪些 entry，只重核那一份，不必全量重比」。

## 本道到此为止的完整状态

一条数据未丢，两个仓（`overview`、`book-index`）都已 fetch 最新 main 合并推送成功，
工作区干净。之前那批还在等重试的 7 条 GLM 请求（收工指令说"别等这批跑完"）
**已按指令放弃**，未产出更多数据，也没有留下悬空的后台任务需要人管。
