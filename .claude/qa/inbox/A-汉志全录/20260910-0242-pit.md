重跑对键（v2 jsonl ＋ 补好的归一表 ＋ 数字/锚点/标点三层判据 ＋ misattached 过滤），
报新三数，跟协调者的 93→38 对过——**基本吻合，多出 1 条新发现，接着做第三批，不停。**

## 新三数

- **对得上、可以直接写**：576 条（含 14 组「库中数字与阿拉伯数字两条重出、同一 Work」
  的情形——两个 `indexed_by` 元素都写；含 `蒼頡一篇`／`蒼頡傳一篇` 那对——`clean_key`
  把「傳」当角色后缀剥掉致假撞键，用原始字符串精确一一配对解开，非误判）
- **真正待核（同 key 落在不同 Work，不写）**：4 组 5 entry——`傳四十一篇`／
  `別字十三篇`／`揚雄所序三十八篇`（各有 2 个库 Work 同题，1 entry 不知该配哪个）／
  `龐煖二篇`（jsonl 两处、库只 1 条，可能库漏收或本是互见）
- **剩余（entry_only，真差异或已排除）**：41 条 ＝ 协调者裁的 **38** ＋
  **misattached 排除 2**（周官經六篇／山海經十三篇）＋ **新发现 1**（见下）

## 新发现的第 1 条：`鍾`／`鐘` 也是一对缺口，且带一个数字疑点

整理本 `鍾律叢辰日苑二十二卷`，库中 `d59f2pvjww74` 作
`鐘律叢辰日苑二十三卷`／`鐘律叢辰日苑23卷`——**`鍾`／`鐘` 未进归一表**（补的 21 对
没有这一对），**且卷数不同（二十二 vs 二十三）**。字形归一表没这一对**不奇怪**——
它和 §四已知的 11 条「形近异文」是同一型：**这条卷数不同，不是异体字问题，
入表也配不上**（该表只收字形，见 12 档 §四末段的判准）。按第四型「形近／异文，
须翻影印本」处置，**列入清单不改**，不单独再报——协调者裁决文档已把「异体字表
不收异文」的判准立清楚，这条照此判准自动落位，不需要再等一轮裁决。

其余数字与协调者的 38 完全对得上（4 型：多书合行 4／题带附注 7／撰人黏题首 5／
形近异文 11＋这条新的=12／库中确无 11，合计 39，加 misattached 排除的 2 条＝41）。

接着做第三批（100 条），不停。
