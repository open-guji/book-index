第一批（核兼容性＋对键）踩坑与发现，一条数据未改。

## 1. `黄`／`黃` 不在任何异体字表里（`pit`，建议协调者裁是否补表）

对《漢書藝文志》整理本 622 條目与库中 659 條著錄用 `norm_shared.clean_key()`
对键时，93 条整理本条目在库中找不到对应，其中 **20 条唯一的差异就是
`黄`（简体形部首）对 `黃`（库中写法）**，例如整理本「黄帝內經十八卷」
对库中「黃帝內經十八卷」。查了 `scan.py` 的 `VARIANTS`（43 对）与
`norm_shared._TITLE_EXTRA`（18 对），**都没有这一对**。

不敢自己往 `scan.py` 加（工具冻结，只协调者可改），也不确定这该算「異體字」
还是该算「简繁」（如算简繁，`opencc` 类转换或许已经处理，值得查一下现有
`scan_simp.py`／s2t 检查为何没在这批影印本重建时纳入 `黄`）。先报，
裁了再动。

## 2. 整理本（上游产物）里两类值得报给整理本那道或协调者的现象（`cross`）

只读整理本，没改：

- **校記锚点混进了 `entry.title` 字符串**：`禮古經五十六卷，經七十篇①`／
  `長孫氏說二篇①`／`大命二十七篇①`／`大夫種三篇①` 四条，`①` 是校記的锚点，
  夹在题名字符串里。对键、日后任何拿 `title` 做展示或匹配的用法都会连着印出这个
  圈码，建议整理本那道确认是否该挪到别处（如单列一个 `jiaoji_ref` 字段）。
- **一行原文排了多部书、整理本按行收作一个 entry**：如
  「古雜八十篇 雜灾異三十五篇 神輸五篇圖一」「孟氏京房十一篇 灾異孟氏京房
  六十六篇 五鹿充宗略說三篇京氏段嘉十二篇」，库中这几部书是分开单条著錄的。
  第三批处理到这几条时我会人工拆开逐部对应，这里只报现象，不需要协调者裁决。

## 三、对键结果概要（详见 `目录录入/二十五史艺文志考补/整理本/漢書藝文志/A-汉志全录-对键与兼容性.md`）

- 一对一对上：521 组（entry 521 ↔ 库著錄 521）
- 一对多／库内本重待人核：6 组（entry 8 ↔ 库著錄 11）
- 库有整理本无：127 条（不动库）
- 整理本有库无：93 条（不新建）

兼容性核实：**兼容，往下做**（dataclass 无往返问题、`reindex.py`/`scan.py`
均不受影响，`validate_entries.py` 本会话未找到该文件，无法核实，风险判断见上）。

接着做第二批（30 条试水），不停等回复；`黄/黃` 那条裁决不影响第二批（试水
条目不含黄字那 20 条即可绕开）。
