第三批起（100 条一批，连着做完不停）**已全部落地：576 条全部完成。** 报数与收尾。

## 一、落地总账

| 批次 | 条数 | 备注 |
|---|---:|---|
| 第二批（试水） | 29 | 1 条（`周官經六篇`）因 `misattached` 撤销 |
| 第三批 | 100 | |
| 第四批 | 100 | |
| 第五批 | 100 | |
| 第六批 | 100 | |
| 第七批（收尾） | 147 | 剩余全部 |
| **合计** | **576** | |

四道闸每批必过，**全部批次都过了**：

1. JSON 合法：每批 100%
2. 越界检：每批的 `git diff` 新增键只有 `text`／`self_note`／`commentaries`／嵌套 `by`。
   期间几次误报（`section`／`section_basis`／`note` 出现在 diff 里）都核实为**紧邻插入点
   的既有末键被补上一个逗号**，值本身未变——这些都是该 Work 已有的「同一漢志、
   两条 `indexed_by` 重出已合并」的旧案（`merged_from`／`note` 字段自带说明），
   本道往两条重出条目里各写了一份相同内容，不算越界
3. 逐字等同：**全部批次、全部写入点，差 0**
4. 抽样人工核：每批抽 20 条，累计抽了约 100+ 条，**0 处归属错乱**

## 二、全库收尾核对（自洽性交叉验证）

```
漢志著錄总条数 659
= 已写新字段 590
+ misattached 排除 2（周官經六篇／山海經十三篇）
+ 真正待核（不写）7（傳四十一篇×2／別字十三篇×2／揚雄所序三十八篇×2／龐煖二篇×1）
+ 库有entry无（lib_only）60
```

**590+2+7+60=659，账平。**`reindex.py`／`verify.py` 每批之后都跑了，全绿，
`裁決賬（只增不減）` 稳定在 6848（本道未新增裁决记录，符合预期——本道不做并条判断）。

## 三、留下的两小片，供协调者或后续道处置

1. **7 条真正待核**（同 key 落在不同 Work，1 entry 不知该配哪个）：
   `傳四十一篇`（`d59f23nxnmyq`／`d59f1irabaps`）、`別字十三篇`（`d59f23nqh6gw`／
   `d59f206kbcow`）、`揚雄所序三十八篇`（`d59f23nowzrc`太玄／`d59f23nolr7l`法言）、
   `龐煖二篇`（诸子从横／兵书兵权谋两处引，库只 1 条 `d59f2evlzwn9`）。
   这几条不复杂，**建议协调者直接裁**（多半靠 `section` 或查一下该 Work 现有的
   `authors`/`period` 是否与《漢志》所属类别相符即可定），本道不越权代裁。
2. **41 条剩余**（entry_only）已在 `20260910-0242-pit.md` 报过：38 条与协调者
   `12-对键判据四缺口.md` 的四型吻合，2 条 misattached 已排除，1 条新发现
   （`鍾/鐘` 异体＋卷数不同，归入「形近异文待翻影印本」型，不单独处置）。

## 四、第四批（`xu`／`lei_total`／`lue_total`／`jiaoji` 归属）——写 `ask`，不擅自定

v2 jsonl 里这四类非条目元素的量：`xu` 45、`lei_total` 38、`jiaoji` 19、`lue_total` 6，
合计 **108 个元素**，都不属于任何一条 Work。任务书原文要求「先出方案写 `ask`」，见下。

接下来会另写一份方案单列候选与取舍，本单先报「576 条已完工」这件事，
不等方案单批复，本批的收尾工作到此为止。
