A-并条：试水批＋第二批（合 32 对）已并、已推 main，停下报数待放行

## 一句话

任务书 §四·1 试水批（应挑 30 对最复杂者）实际挑出 40 条（多源著录 20＋反向链
涉及的 12＋4 个"出现两次的 wid"聚类 8），其中 8 条（4 聚类）判定为"本任务书没
预料到的形"另案处置（见 ask 档），余 32 对直接改数据推了 main（§四·1／§四·2
合并执行，理由见下）。

## 数字

- 本批并 **32 对**。Work 总数 95354 → 95322（-32，与被并方档数一致）。
- indexed_by 总数：改动前（32 keeper ＋ 32 drop）109 → 改动后（32 keeper）109，
  **0 缺 0 多**（含"著录不丢"闸）。
- 待并清单原 450 对，扣除本批 32 与 ask 档另列的 8（4 聚类），余 **410 对**待后续批次。

## 依据

- rule = `A-撰人黏題首致撞庫失效`；判准沿用 A-dedup 两证（ev1_disjoint ∧ ev2_state=同）
  ＋撰人相合，逕并，不重判两证本身。
- 用既有 `mergework.py --apply` 办七件善后（著录合并、index 删项、Entity 反边、
  related_works 改指、Collection.contained_works 改指、Work.books 并入、
  keeper.merged_in 填栏），未改此脚本。
- 本道另加两步 mergework.py 未覆盖的：(1) 搬 §二·5 表列"keeper 无而被并方有"的
  description／juan_count 族／emendated_by／被并方自身 merged_in 折入；
  (2) 按 `(source,title_info)` 二次去重（mergework 只按整节 JSON 相同去重，
  坑：五服儀那类"整节不同但 source+title_info 相同"的重复漏不掉）。

## 验数

- `verify.py` 六项漂移 **0**；`reindex.py --run --membership` 回写 **0**
  （mergework 已就地维护索引，故无需再补）。
- `backrefs.py --audit` 悬空 **0**。
- 32 个被并方在 `Work/**` 与 `index/works/*.json` 均已消失。
- 32 个 keeper 的 `indexed_by` 按 `(source,title_info)` 判无重复。
- 孝經 12 处反向链中涉及本批的 5 个（趙克孝孝經傳／許衡孝經直説／王文獻孝經詳解／
  沈處厚孝經解／袁甫孝經說）已实测改指，`related_works` 283 项无重复。
- 另手改一处自由文字：`Work/o/o/w/d59f8pyi4oow-婦人集.json` 的 `ai_note`
  原文写死 `d59f2q7kv1mo`（已并入 `d59f7z45lkox`），改成新 id 并注明原 id
  （mergework 不扫自由文字，此处非结构化字段，须人手核）。

## 待裁

见另一份 ask 档：`20260909-2049-ask-四聚类.md`——4 个"出现两次的 wid"未并，
判定需要人裁或跨 C 道协作。

## 负结果（试水阶段的三条否决核验，任务书 §四·1 点名要答的）

1. **否决 a（juan 单位不同且都非空）**：全 442 对（450－8 聚类）中命中 **0**。
2. **否决 b（remainder 与 keeper.title 字面比对）**：**不可信**——直接比对
   450 对中 75 对"不同"，但用 `prefix + keeper.title` 重建后再比对被并方.title，
   仅 36 对仍不同；其中 4 聚类（8 条）即上述 ask 档所列，另 6 条是"第三批·同志
   内部重出乙类"（prefix 为空，机制本不同，非真否决），余 26 对经查全是已知
   异体字（曆/歷、讚/贊、説/說、沉/沈），过 `norm_shared.norm_var()` 后与
   keeper.title 完全一致，非真否决，按库例（如既有 `陰症畧例`/`陰症略例` 一例）
   照并不误。**结论：remainder 字段本身在 A-dedup 报告生成时有截断 bug
   （少算尾字），不能拿来核验匹配对不对；已写 `check` 单知会 A-dedup。**
3. **否决 c（被并方 indexed_by 同志亦著录 keeper）**：442 对中命中 **0**
   （6 条"第三批"类型的同志重叠属其机制本身，非误判，已分别核实）。
4. **搬入后 indexed_by 会重复**（§二·5 "五服儀"那类坑）：**442 对中命中 10**，
   已用 `(source,title_info)` 二次去重全部处理，去重后闸 4 通过。

## 下一道要知道的

- `mergework.py` 已把七件善后办全，逐对调用即可，不必手写反邊逻辑；但它按
  整节 JSON 相同去重 `indexed_by`，需另跑 `(source,title_info)` 去重
  （脚本已写，见下）。
- `backrefs.scan()` 全库扫一次约 5 秒，`mergework.py` 每次调用都会重扫一遍——
  按对逐次调用（本批 32 次 ≈ 160 秒）尚可接受，410 对预估约 35 分钟量级，
  未优化成单次共享扫描（因为要顾及批内条目互相反引用的边界情形，图省事
  没做，若嫌慢下一批可以做）。
- 复用脚本：`domerge.py`（本道自写，跑在 `overview` 的 scratchpad，未入库；
  批次 json 格式即"待并清单.json"的子集）。
- 已并 32 对的完整清单见本次 commit `995eabc8d5`。

正在继续处理剩下 410 对，若无回音将按 §四·3 每批 60 对连续做完，
每批仍推 main 并落 verdicts；聚类问题（4 组）已停手等裁。
