A-并条：收工——450 对待并清单全部有归（446 对已并、4 对未并转 C 道/待审）

## 做了什么

执行 A-dedup 道判过的"撰人黏题首致撞库失效"450 对待并清单的合并，按协调者 §八
裁决办完 4 聚类的歧义部分，全程用库里既有 `mergework.py` 办七件善后，自补
`(source,title_info)` 二次去重与 §二·5 字段搬运，逐批过六道闸后推 main。

## 数字（带分母）

- 待并清单 450 对（446 个唯一被并方 wid，450 个唯一 keeper 候选）。
- **446 个 Work 档已删**（Work 总数 95354 → 94908，Δ=446，与本道全程删档数**精确相符**）：
  - 试水批+第二批：32 对
  - 协调者裁定判据一/二两组三方合并（养羊法、古文孝經傳）：各删 2 档 = 4 档
  - 第三～九批（剩余 410 对）：410 档
  - 合计 32+4+410 = 446
- **4 对未并**（王褒象經×2 行、郭璞穆天子傳×2 行，涉 2 个唯一 wid）：
  - 郭璞穆天子傳 → `cross` 单转 C 道（两候选 entity_id 不同，且一候选 authors.name
    未剥役字）
  - 王褒象經 → 待审档（两候选 role 相反，疑候选一缺撰人"周武帝"）
- 落账 `verdicts.jsonl` 共 **446 条**（check=I，rule=A-撰人黏題首致撞庫失效，
  含判準一/二两个变体），另加候选内部先并的 2 条（养羊法/古文孝經傳的 K2）。

## 依据

- 两证判准（ev1_disjoint ∧ ev2_state=同）沿用 A-dedup 既有结论，本道不重判。
- 协调者 §八 四条判准处置 4 聚类的歧义部分（判准一：entity_id同+志不相交硬判并；
  判准二：役字型判并；判准三：entity_id不同转C道；判准四：role矛盾或authors不全
  立待审）。
- 七件善后全部走库里既有 `mergework.py --apply`，未改动此脚本或任何冻结工具。

## 验数

- 每批次跑 `reindex.py --run --membership` ＋ `verify.py`（六项漂移全程 **0**）
  ＋ `backrefs.py --audit`（悬空全程 **0**）。
- Work 总数减少 446，与删档数精确相符（无静默多删或漏删）。
- 32＋410 对逐批核对"著录不丢"（改动前后 `indexed_by` 元素总数一致，减去
  显式去重的部分）。

## 待裁（已写、已挂起）

1. `inbox/A-并条/20260909-2115-cross.md`——郭璞穆天子傳，转 C 道。
2. `overview/…/待审元数据/A-并条-20260909-象經注撰人角色相反.md`——王褒象經，待考据。
3. 协调者已量出的 **40 簇 / 80 条**同型盲区（entity_id同+题名归一同+志不相交而
   从未检出的重出）——协调者已表示"另排，不在本道范围"；本道若有余力会
   另出一份只读分类清单（见 §八·三），不改数据。

## 负结果（试过没用的和成功的一样值钱）

1. **remainder 字段截断 bug**：A-dedup 报告生成时的 `remainder` 字段有截断，
   不能直接拿来核验匹配对不对（75/450 字面不合，其中仅 4 组是真问题，
   26 组是异体字，20 组是"第三批"机制本身如此）——协调者已认，知会 A-dedup 线。
2. **mergework.py 两处未覆盖的字段搬运**（本道自行在 `domerge.py` 补齐，未改
   `mergework.py` 本身）：
   - `additional_titles`：只搬 drop 的 `title` 本身，不搬 drop 已有的
     `additional_titles` 列表——已回查第一批 32 对，追加修复 3 处（賦門/五題/
     嶺南腳氣論）。
   - `indexed_by` 去重按整节 JSON 相同，不按 `(source,title_info)`——442 对中
     实测命中 10 处需要二次去重，已全部处理。
3. `backrefs.scan()` 逐对重扫（每次约 5 秒）未优化为单次共享扫描——协调者已确认
   "可以接受，不必优化，省下的时间不值那个风险"。

## 下一道要知道的

- 复用脚本：`domerge.py`（写在 A-并条 的 scratchpad，**未入库**，若下一道要接手
  同型合并，建议把它整理进 `book-index/.claude/qa/`，需经协调者批准后由协调者
  改动，因工具冻结）。核心两点：(1) `mergework.py --keeper K --drop D1,D2,...`
  支持一次调用办多个候选合入一个 keeper；(2) 合并后务必按
  `(source,title_info)` 再去重一次，`mergework.py` 自身的去重不够。
- 常态：`period`／`period_basis`／`period_upper`——断代是另一族，本道全程未碰，
  被并方原有 period 而 keeper 无时也未搬（按任务书交底，留断代道核）。
- 坑：孝經类反向链涉及 5 处（趙克孝孝經傳等）已实测走通 `mergework.py` 的
  `related_works` 自动改指，283 项无重复；`婦人集` 自由文字提及需人手改，
  `mergework.py` 不扫自由文字。

## 收尾

`verify.py --strict`（当前无 `--strict` 参数，用默认已含全项）全程 OK；
本道写域内改动全部已推 main；`调度/排班.md` 待协调者改本道状态为"已收工"
（本道写域内不改排班表，遵 README 规矩四）。
