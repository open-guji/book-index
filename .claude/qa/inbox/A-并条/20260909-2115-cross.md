A-并条：cross —— 穆天子傳／穆天子傳注 entity_id 不同，疑人物侧有重复，建议归 C 道

## 一句话

被并方"郭璞穆天子傳"（d59f2q6ga6m8，新唐書藝文志）撞上两个候选，`entity_id` 不同：

- `d59f27yyjj0h`《穆天子傳》：`authors=[{"name":"郭璞注","role":"撰",...,"entity_id":"hixhjxrrxl34"}]`
  ——**`name` 字段本身写成"郭璞注"，役字"注"未剥入 role（坑 20 应剥而未剥）**，
  role 却记作"撰"，与 name 内含的"注"自相矛盾。
- `d59f27yw1mo3`《穆天子傳注》：`authors=[{"name":"郭璞","role":"注",...,"entity_id":"hixhd2h9bhm7"}]`
  ——这条剥得对（name=郭璞，role=注）。

两候选都是"郭璞注《穆天子傳》"这同一件事，**却系着两个不同的 entity_id**，
且其中一个候选自身的 authors.name 有既存的役字未剥缺陷。

## 建议 owner

C 人物体系道——需先核 `hixhjxrrxl34` 与 `hixhd2h9bhm7` 是否为重复人物（都指郭璞），
若是，按人物侧惯例合并 entity 并回填两条 Work 的 `authors[].entity_id`；
同时 `d59f27yyjj0h` 的 `authors[0].name="郭璞注"` 需改回 `name="郭璞"`
（役字入 role，已有 role=撰 但应为 role=注，需一并核）。

## 本道处置

协调者已裁（任务书 §八·二·三：entity_id 不同者转 C 道，本道一个字都不动）。
被并方 `d59f2q6ga6m8`（郭璞穆天子傳）本道跳过，留在库中原样，
待 C 道裁完 entity 之后再回来补办这一对的合并（届时套 `mergework.py` 即可，
流程与本批其余对完全一致，不必另写脚本）。

## 数字

本 cross 涉 3 个 Work id（被并方 1 ＋候选 2）与 2 个 Entity id，暂不改动任何一个。
