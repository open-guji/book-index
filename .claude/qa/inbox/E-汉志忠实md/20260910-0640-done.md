# 收工单：本道办竣，全部已推 main；三件交办事项状态与「通用解析器硬骨头」都写进任务书了

**发**：E-汉志忠实md 道（`session_01FR4Ny5JqFM2T8YZiM3WkTM`）
**推**：`overview` `238b374`　｜　任务书新增节：`进度/E-文本整理本/任务书-E-汉志忠实md.md` §「收工时状态（2026-09-10）」
**不用叫醒协调者。**

---

## 一、三件交办事项：**全部办竣**

| # | 事 | 状态 |
|---|---|---|
| ① | 末尾整理者按语改注释 ＋ 重跑两道闸 | **办竣**。正文层 **21 575 / 21 575 差 0**；全文件 **21 765 / 21 765 差 0**；解析回归 **710 条不一致 0** |
| ② | 元素类型清单落成表 | **办竣** → [`07-元素类型清单.md`](../../../../overview/项目进展/古籍索引网站/进度/E-文本整理本/07-元素类型清单.md)（汉志全书 9 类元素逐类定处置） |
| ③ | 既有整理本里几部底本是「夹注小字排」 | **办竣** → `06-既有整理本底本形态摸底.md` |

字数逐项算平：

```
22 200 − 397（页末校記）− 38（圈码）= 21 765（OCR 正文區）− 190（书尾按语）= 21 575（志文正文层）
```

## 二、⚠️ 请更正方针档一处

`05-二十五史考补全面改新格式.md` §二·摊一 写「`E-汉志忠实md` 道已受命量这个数，**未交**」——
**已过时**：该档 **02:5x 就推了 main（`c3831b5`）**，早于方针档。

**答案是 0，而且这个问题问错了对象**：既有 **64 部没有一部从影印本 OCR**
（維基文庫 14／知乎 15／識典古籍 5／kanripo 2／未注明 28；`grade: rough` **45**）。
含 `⟨…⟩` 者只 2 部：《千頃堂書目》9 392 处、《漢書藝文志》186 处。

**摊一的分母因此要重定义**：不是「几部底本是夹注小字排」，
而是「**几部值得从影印本重做**」——`rough` 那 45 部里有影印本源的才是。**这个数本道没量。**

**最省事的下一步**：《千頃堂書目》9 392 处夾註**已经标好**，
不需要影印本也不需要重跑 OCR，**拿它当第二个卷验证解析器的可配置性**，比等第 4 卷 OCR 快得多。
⚠️ 它的注家是明代书目那一套，**白名单必须重建**。

## 三、协调者点名要的那一句：**通用解析器的三处硬骨头**

全文在任务书 §三，要点：

1. **版面参数不能人手填进配置，必须自动标定。**填错一个字距，DP 会**整段错位而不报错**。
   配置里存的必须是 `calibrate.py` 的产物，**每卷跑一次，十分钟，绝不许照抄上一卷**。
2. **续行规则最难参数化，而且错了不报错。**这是我调得最久的一处（换了三版）。
   它不是参数，是**一段判断逻辑**；配置只能「选哪套策略 ＋ 该策略的参数」。
3. **最要紧：没有内生计数句的卷，必须另找一个自洽量。**
   汉志能查出续行规则的错全靠 38 个「凡X家／右X家」逐一对账。**别的书未必有。**
   没有它，续行错了**没有任何一道闸抓得到**（逐字等同抓不到——字没丢，只是并错了行）。
   **每卷开工第一件事就是先找到它的自洽量；找不到就人工抽样，且抽样比例要写进报告。**

配置样板（YAML，一卷一档）已写在任务书 §三·乙：底本／版面／续行／自洽量／第二层／元素处置／字形 七节。
**配置装不下、必须每卷人做一遍的两件**：元素类型清单（那是判断，不是参数）、逐行订正表
（但**发现它们的三道检完全通用，直接搬**）。

## 四、中间产物：**已入仓，别再当它在临时目录里**

`目录录入/二十五史艺文志考补/整理本/漢書藝文志/中间产物/`（**1.4 MB**）：
`hz_json/` 50 档（**55 分钟 CPU**）＋ `hz.jsonl`／`hz_fn.jsonl` ＋ 一份说明。

依据是收工令那两句：「别删它」「没推的就是丢了」。会话一归档临时目录就没了。

⚠️ **「该不该往仓里放 OCR 中间产物」仍然没人裁过。**本道按「宁可留下、一条命令可撤」处理，
**若裁为不该收：`git rm -r 中间产物/` 即可**，不影响任何产物。

`vol01.pdf`（40MB）与 `hz_img/`（25MB）**已删** —— 该删的就是这两样，几分钟可重建。

## 五、顺手消掉一处重号

我的 `05-元素类型清单.md` 与协调者方针档 `05-二十五史考补全面改新格式.md` **撞号**，
已改为 **`07-元素类型清单.md`**，两处引用一并改过。

## 六、待裁五项（全在任务书 §四）

1. **`03-夹注渲染核验.md` 说 4 365 处 `⟨…⟩`，本道实测 9 578 处** ——
   两个数都没采信。**03 是 E 赛道前置，分母不对后面核验就都不对**，建议先对齐。
2. 两个字留 `□` 未猜：p40「歹旁凶」（码位未定）、p47 服虔注二疒部字。
3. `蹴鞠` 取 `蹴`（依据：同书注文里 OCR 自己读出的那一处）。
4. **与库中旧整理本（四庫本底本）对校未做** —— 本份是百衲本、带顏注与校記，是第二个见证本。
5. 顏注入不入库 —— 用户待裁项。

## 七、本道全程边界

`book-index` 条目数据一个字没碰（只写 `.claude/qa/inbox/` 下 4 个档）。
`book-text` 匿名只读 clone，无 attach、无推送权，既有 64 部整理本一个没动。
ctext 一个字节没取。
