# ask：Book `98jhht5r0h` 的 title「李子」與 work_title「季子」不符

- 道：`先秦-輯本藏所`
- 涉及：Book `98jhht5r0h`（`title="李子"`），work_id `d59f2evy65ts`（`title="季子"`）

## 事由

清單〈收尾甲-辑本Book無藏所.tsv〉第 44 行 `work_title` 作「季子」，但其對應之
Book `98jhht5r0h` 的 `title` 欄卻是「李子」。Book 的 `ai_note`（先秦-輯本與佚書道
2026-09-14 建）稱：「兵權謀家《李子》十篇，清孫星衍有輯本」——看來輯本本身確指
兵家《李子》，但所屬 Work 卻叫「季子」，二者聲旁形近，疑是本庫早先錄入時的別字，
也可能「季子」「李子」本是異名同書（尚待查《漢書藝文志》兵權謀家原文核實）。

## 為何不自己動手

`title` 屬 Work 層欄位，不在本道寫域（`contained_in`／`edition`／`resources`）之內，
先秦收官 §三·邊界亦明令「不碰 Work 層任何字段，非有實據不許改」。本道只核實了
「輯本內容為兵家《李子》」這一層，未觸及 Work 的 `title` 該定為「李子」還是「季子」。

## 建議裁法

1. 查《漢書藝文志·兵書略·權謀家》原文，確認該篇著錄題名究竟是「李子」還是「季子」
   （或本庫收錄的是《呂氏春秋》一類另有其人的「季子」，與兵家「李子」根本是兩個人，
   則屬「同 work_id 誤繫」而非「別字」，性質更嚴重，建議一併排查 `related_works`）。
2. 裁定後屬丁道寫域（`title`），本道之 `contained_in`／`edition` 判斷
   （乙檔：繫平津館叢書，帙卷未考）不受此裁定影響，兩案可分頭辦理。

已落 `verdict=open`（`.claude/qa/verdicts.jsonl`，`id=98jhht5r0h`, `check=T`,
`rule=T-title與work_title不符待覆核`）。本道不停工，繼續辦其餘 55 條。
