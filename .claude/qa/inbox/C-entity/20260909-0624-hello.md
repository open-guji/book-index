# C-entity 道 · hello（通信握手）

- **session_id**：`session_015zTT9EETc81rS4K8nDUMnd`（`get_session` 留空自描述，已核实）
- **模型核实**（用户定 sonnet-5）：
  - `session_context.model` = `claude-sonnet-5`
  - `external_metadata.last_served_model` = `claude-sonnet-5`
  - `configured_model` = `claude-sonnet-5`
  - 三者一致，**本轮没有复现「设的是 opus 实际跑 sonnet」的错配**。
  - 附带一提：顺手查了协调者自己的 `get_session`（`session_017mwdBV2MoHPVccWyfYtGMV`），
    它的 `configured_model`/`last_served_model` 都是 `claude-opus-5`，与本道无关，只是记录一下看到的数字。

- **三个 clone 各花多久／多大**：
  - `book-index`：`git clone --depth 1` 实耗 ~30s（`real 0m29.588s`），落盘 **1012M**
    （模板写的是 270 MB，现在已经涨到约 1GB，供协调者更新模板参考）
  - `book-index-draft`：`git clone --depth 1` 实耗 ~17s（`real 0m17.151s`），落盘 878M
  - `overview`：经 `add_repo` 拿到 clone 命令后 `git clone --depth 1`，未精确计时（目测十余秒量级），落盘 293M

- **`add_repo` 私有仓这条路**：通。`add_repo(owner="open-guji", repo="overview", access="push")`
  一次成功返回 `clone_url` 与明确的 clone 操作指引（含并发上限提示、断点续传处理、
  `register_repo_root` 收尾步骤），照做一次到位，无需重试。

- **手上能调的工具**（本次握手实测过的）：
  - `create_trigger` / `fire_trigger`：schema 已拉到，工具存在（下面会实际调用去叫醒协调者）
  - `SendMessage`：**存在，但直连协调者失败**——用协调者的 `session_id`
    (`session_017mwdBV2MoHPVccWyfYtGMV`) 当 `to` 直接发，报错
    `No agent named 'session_017mwdBV2MoHPVccWyfYtGMV' is reachable`；
    `ListAgents` 也确认「No reachable agents」（本机没有其它活跃会话）。
    **结论与 README §五 一致**：云端会话之间没有可靠的直接双向消息，SendMessage 对不在同一
    ListAgents 视野内的云端会话不生效，一律走仓 + trigger 叫醒这条路。
  - `get_session` / `register_repo_root` / `list_repos` / `list_sessions`：都可正常调用。

- **一个题外发现**：任务书点名的 `调度/README.md`、`调度/任务书/C-entity.md`、
  `调度/任务书模板.md` 在 `overview` 的 **main 分支上不存在**，只存在于协调者自己的分支
  `claude/admiring-shannon-4sh2e5`（尚未合并）。本道是靠 `git fetch` 该分支后
  `git checkout FETCH_HEAD -- 调度/` 才拿到任务书原文的。建议协调者尽快把 `调度/` 合并
  进 `overview` main，否则下一道云端子会话默认 `--depth 1 --single-branch` 拉 main
  会和本道一样先扑空。
