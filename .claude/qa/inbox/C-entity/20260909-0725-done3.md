# C-entity 道 · done3（第三批：A 档字段比较 + B/C 给倾向 + 反向验证）

## 做了什么

按协调者第三批三件事逐条办：①A 档 4,489 条逐字段比较分 A1/A2/A3；
②档 B 3 条 + 档 C 36 条逐条给倾向，合成一份待审档一次交人裁；
③反向验证 4,489 条共享的 work_id，确认其 `authors[].entity_id` 全部指向
production 侧而非 draft 侧。全程只读，未改任何数据。

## 数字（改前 → 改后，带分母）

### 任务 1：A 档字段比较（分母 4,489）

| 类 | 条数 | 占比 |
|---|---:|---:|
| A1 纯冗余 | 4,379 | 97.6% |
| A2 有增量 | 77 | 1.7% |
| A3 相抵触 | 33 | 0.7% |

A2 增量字段分布：`alt_names` 60、`cbdb_id`/`cbdb_match`/`cbdb_source` 各 33（同批）。
A3 全部列出：32 条纯 `dynasty` 冲突（draft 宋/金 vs production 明，其中 1 条
另带 `cbdb_id` 冲突），详情见摘要单与全量 JSON。

### 任务 2：档 B（3）+ 档 C（36）= 39 条给倾向

归六组：C1 泛称已解繫 9、C2 书名误判为人名 4、C3 批量入库误加已撤 4、
C4 明太祖改繫他 entity 1、C5 已判非同人 1、C6 双侧皆空无历史 17，
另档 B 3 条（含李文鳯疑与 C3 同类）。倾向明确的 26 条（C1+C6），
需人核的 13 条（B 3 + C2/C3/C4/C5 共 10）。

### 任务 3：反向验证（分母：4,489 条 A 档合计 5,884 个共享 work_id 实例）

**5,884/5,884 全部正确**——`authors[].entity_id` 无一指向 draft 侧，
也没有"两侧都不指"的情形。0 例外，没有断链风险。

## 依据

- A1/A2/A3 判据、三档判据：均为协调者本轮裁决原文。
- 字段比较范围（`dynasty`/`native_place`/`birth_year`/`death_year`/`cbdb_id`/
  `cbdb_match`/`cbdb_source`/`alt_names`/`ai_note`）：协调者裁决一原文点名字段。
- B/C 分组根因：读候选 entity 自身 `ai_note`（多为此前其它道留下的裁决记录，
  如"泛稱 entity 解繫"2026-09-05、"lane-B 併條善後"2026-09-08），非本道臆断。

## 验数

- `entity_a1a2a3.py` 自检：A1+A2+A3+加载失败 = 4,489，与档 A 原计数一致。
- 反向验证脚本自检：5,884 个共享 work_id 实例逐一查过，0 异常。
- `book-index` `verify.py --strict`：五项仍 0，裁决账只增不减（见推 main 后的验数）。

## 待裁

`待审元数据/C-entity-20260909-三档B与C共39条.md`——39 条，一次交人。

## 负结果

- A3 深查了"曾于拱"一例：draft/Work.authors 的"宋/金"与 production 的"明"
  不一致，根因是 **production 侧经过一轮 2026-09-06 的 CBDB 断代校正而 draft 侧
  没跟上**，不一定是"两个不同的人"。**但这只是一例的深查，本道没有逐条查完
  其余 32 条是不是同一种成因**——协调者原话"这类往往意味着不是同一人，或有
  一侧是错的"，两种可能都要留，不能因为查到一例像是"回填未同步"就假设
  全部 32 条都是同一回事去批量处理。
- 想把档 C 的 17 条"双侧皆空"归为"可直接删"，但没有查过它们是否在原始志书
  里其实各有著作只是本轮还没收录——只敢说"以本轮所见信息，双侧皆空"，
  不敢说"这些人名下真的什么书都没有"。留给人核作最终判断依据之一，
  不是本道替他们把这层不确定性抹掉。

## 下一道要知道的

**併條那道开工前该读这几份，不必重扫两仓**：
1. `overview/scripts/qa/reports/20260909/entity_dup_triage.json`——三档原始分类
2. `overview/scripts/qa/reports/20260909/entity_a1a2a3.json`——A 档字段比较全量
3. `待审元数据/C-entity-20260909-三档B与C共39条.md`——B/C 39 条倾向与根因分组
4. `待审元数据/C-entity-20260909-draft活条大面积重出.md`——第一批背景（已裁"甲"）
5. 反向验证已确认 4,489 条无断链风险，A2 的 77 条**须先搬字段再摘**（`alt_names`/
   `cbdb_id`/`external_ids` 三键），A3 的 33 条**一条都不许自动办**。

**上下文用量 34.4%（343,762/1,000,000）**，未过六成，仍有余量，如需第四批可继续排。
