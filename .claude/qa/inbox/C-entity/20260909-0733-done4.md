# C-entity 道 · done4（第四批：标记方案查惯例 + A1/A2 执行清单 + 206 条画像）

## 做了什么

三件事，全程只读、未改任何数据：①查库内既有惯例，给"判为重出的 draft 该怎么标"
一份方案（不执行）；②A1/A2 的可执行清单与分批方案（仍不执行）；③206 条真正
"非同名"活条的画像。

## 数字（改前 → 改后，带分母）

### 任务 1：标记方案（无新增数字，查证结果见方案文档）

- 现行主动写入的併条字段：`merged_from`（`merge_duplicate_works.py` 等）、
  `merge_history`（同）；`merged_in`／`merged_into` 是历史遗留，无现行写入者。
- `book_index_manager.promotion.validate_promotions` 的 E07 检查逐行读过，
  确认它专门处理"多条 draft 併入同一 production"且专为 Entity 设计了
  `primary_name`＋`alt_names` 判据——是目前查到的最直接先例。
- 顺带核实：档 A 4,489 条里，**没有任何一个 production 候选被 ≥2 条 draft
  同时命中**（0 例外），排除了"E07 一对多分支会在本批触发"的顾虑。

### 任务 2：A1/A2 执行清单（分母 4,379 + 77）

- A1 4,379 条：扫了全部出现过的键，**比较范围外且有非空值的键：0 个**
  ——"无信息损失"判断没有漏字段。
- A2 77 条：逐条确认"搬字段会不会与 production 已有值冲突"——**0 冲突**
  （分类算法设计上已排除该情形，本批验证符合预期）。
- 分批方案：A3 全程不进入执行批次；A2 单独一批（77 < 200）；A1 切 22 批
  （21×200 + 1×179）。

### 任务 3：206 条画像（分母 206 = 4,734 − 4,528）

| 维度 | 数值 |
|---|---:|
| 缺 cbdb_id | 118（57.3%） |
| 缺 dynasty | 164（79.6%） |
| works[] 空/单书/多书 | 1／181／24 |
| primary_name 异体/简体字命中 | 0 |

## 依据

- 惯例查证：`SCHEMA.md`"ID 类型编码"／"记录之共通字段"两节、
  `promote-work/SKILL.md`、`PROTOCOL-v2.md`、`merge_duplicate_works.py`、
  `merge_entities.py`、`book_index_manager/promotion.py`（`validate_promotions`
  函数原文，非转述）。
- 206 条画像口径与第一批"4,734 条画像"一致，仅换分母，便于对比。

## 验数

- A1 键扫描、A2 冲突检查均为脚本直接计算，非估算（见
  `overview/scripts/qa/reports/20260909/batch4-A1A2执行清单与批次方案.md`）。
- 206 条数字复核：`4734 - 4528 = 206`，与索引直接筛选结果一致。

## 待裁

`待审元数据/C-entity-20260909-重出条目标记方案.md`——三条路线（甲/乙/丙）列出
优劣，本道倾向甲，但方案本身要动 `promotions.json` schema 且需新脚本，
超出本道"只读画像"授权，交协调者裁。

## 负结果

- 一开始以为"档 A 里会不会有 production 候选被多条 draft 同时命中"需要
  当作未知风险写进方案里，查完发现本批**恰好**没有这种情形（0 例外）——
  但已在方案里说清楚"这是本批没撞上，不是路线本身规避了这个问题"，
  没有因为这次干净就把方案乙的长期风险抹掉。
- 查 `merge_entities.py` 想看它有没有可以直接复用的"draft↔production 归并"
  逻辑，结果发现它**连 draft 内部併条都不写任何结构化痕迹**（`os.unlink`
  静默删除）——比 Work 侧的 `merge_duplicate_works.py` 还落后一截，
  不能直接拿来当先例，只能当反例写进方案的"丙"路线里。

## 下一道要知道的

**併條那道开工前该读（累计四批，不必重扫两仓）**：
1. `overview/scripts/qa/reports/20260909/entity_dup_triage.json`——三档分类
2. `overview/scripts/qa/reports/20260909/entity_a1a2a3.json`——A 档字段比较
3. `overview/scripts/qa/reports/20260909/batch4-A1A2执行清单与批次方案.md`——
   执行清单 + 分批方案 + 206 条画像
4. `待审元数据/C-entity-20260909-三档B与C共39条.md`——B/C 39 条倾向
5. `待审元数据/C-entity-20260909-重出条目标记方案.md`——**标记方案，协调者
   裁完路线之后，下一道直接照方案甲（或裁定的路线）写脚本执行即可，
   不必重新调研惯例**
6. `待审元数据/C-entity-20260909-draft活条大面积重出.md`——第一批背景

**上下文用量 38.9%（388,698/1,000,000）**，仍有余量。
