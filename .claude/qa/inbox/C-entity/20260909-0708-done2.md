# C-entity 道 · done2（第二批：三档分类 + qa_common.py 修复 + SCHEMA.md 补全）

## 做了什么

按协调者四条裁决逐条办：①draft 同名 4,528 条按判据分三档，只出数不动数据；
②`qa_common.py` 两处授权修复并三边回归；③`SCHEMA.md` 三处授权补文档；
④知悉新规矩（全量报告不进数据仓 inbox），本批报告已按新址落盘。

## 数字（改前 → 改后，带分母）

### 裁决一：三档分类（分母 4,528，脚本 `overview/scripts/qa/entity_dup_triage.py`）

| 档 | 判据 | 条数 | 占比 |
|---|---|---:|---:|
| A | 同名 ∧ works[] 有交集 | 4,489 | 99.1% |
| B | 同名 ∧ 两侧 works[] 皆非空但无交集 | 3 | 0.07% |
| C | 同名 ∧ 一侧或两侧 works[] 为空 | 36 | 0.8% |

全量清单：`overview/scripts/qa/reports/20260909/entity_dup_triage.json`
（档 A 带共享 work_id、档 B 带两侧 dynasty/birth_year/death_year、档 C 带双侧
works[] 空/非空状态）。摘要+样例另落本 inbox 同名"三档重出摘要.md"。

### 裁决二：qa_common.py 修复 + 回归（分母：各类型抽样 30 已知已 promote 的 draft id + 若干 production id）

- 字段名 `promoted_to`→`_promoted_to`、`promoted_at`→`_promoted_at`（`check_promote_ready_generic`）
- `TYPE_PREFIX_OFFICIAL[Entity]`：`('11','12')` → `('hi',)`
- 回归：entity/work/book 三类型各抽 30 个已知已 promote 的 draft id 跑对应 qa_*.py，
  **PRO04 90/90 全部正确触发**（改前：0/90，因字段名不对全是死代码）
- 反向抽验：production 侧各抽 3 个（entity 3、work 3、book 3，共 9 个）跑 qa_*.py，
  **PRO01 0/9 误报**（确认字段修正没有引入新噪音）
- 顺手在 `qa_entity.py` 里移除了此前为绕开这个 bug 自建的 `PRO05`/`PRO09`
  （已被 qa_common 修好的 `PRO01`/`PRO04` 覆盖），只留 `promotions.json` 交叉核验的
  增量检查 `PRO06`/`PRO07`/`PRO08`（qa_common 本身没有这层）

### 裁决三：SCHEMA.md 补全（分母：production 30,875）

| 字段 | 补的值 | 实测条数 |
|---|---|---:|
| `subtype` | `collective` | 42 |
| `alt_names.type` | 行第/廟號/訛名/異體/封爵/俗姓 | 1407/63/112/163/65/18 |
| `cbdb_match` | 措辞改"自由格式凭据备注，非枚举" | 3,394 条不在字面三值内 |

## 依据

- 三档判据：协调者本轮裁决原文（丁璣四例，"同名不会共享同一本书的同一作者位"）。
- qa_common 字段名：`book-index-draft/SCHEMA.md`"ID 类型编码"一节明文。
- `TYPE_PREFIX_OFFICIAL[Entity]`：`book-index/index/entities/*.json` 全量 30,875 条
  前缀实测（100% `hi`）。
- SCHEMA.md 三处：均为 production 全量实测频次（见上表），例 id 见第一批 check 单。

## 验数

- `book-index` `verify.py --strict`：五项仍 0，裁决账水位线只增不减（跑在下方推 main 之后）。
- 回归详见上"数字"节：PRO04 90/90、PRO01 0/9 误报。
- `entity_dup_triage.py` 跑毕自检：A+B+C = 4,528，与 DUP01 原计数一致，无遗漏无重复计数。

## 待裁

- 三档数字已出，**合并本身未做**（Entity 併條同步五处、断链不报错，是另一个写域，
  协调者裁决一原话"三档的数出来后另行指派"）。
- 档 B 3 条、档 C 36 条需要人工/语义核实，样例已附在"三档重出摘要.md"。

## 负结果

- 一开始想把 SCHEMA.md `alt_names.type` 补成"频次最高的 6 种"，但重新核对后发现
  第一批 check 单里已经报过的 6 种（行第/廟號/訛名/異體/封爵/俗姓）并不是全库频次
  最高的 6 种（真正最高的是 著錄形 121 条，不在这 6 种里，只是恰好没被我第一次
  遍历到）——但因为协调者的裁决三是照着这 6 种数字（"338 条留 WARN"）批的，
  且这 6 种本身也都是实测存在的合法值，不是错的，只是不是"最优的 6 种"，
  所以维持原 6 种不变，没有重新调整白名单去追求"频次最优"——**避免为了好看
  而重新洗一遍已经被批准的数字**。这一点写在这里供协调者知悉，如需换成
  频次最高的 6 种（著錄形/異體/行第/訛名/封爵/廟號），下一批可以改。

## 下一道要知道的

- `qa_common.py`/`qa_entity.py`/`SCHEMA.md` 均已改完并推 main，`qa_work.py`/`qa_book.py`
  未改代码但受益于 `qa_common.py` 的修复（PRO01/PRO04 现在对它们也生效了）。
- 三档分类脚本 `entity_dup_triage.py` 可直接复用（只读，跑一次几秒钟），
  下一道如果要处理档 A 的 4,489 条合并，这份清单就是起点，不必重扫。
- **上下文用量 27.8%（277,589/1,000,000）**，还有余量，可以排第三批。
