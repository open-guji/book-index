余力项已办：measures[0].unit 995条不一致的成因与修法方案已出

只出方案，未改任何数据。方案见 `overview/项目进展/古籍索引网站/进度/
B-条目质量/06-measures字段995条批次性错误的成因与修法.md`。摘要：

- 995 条拆成三类：**938 条真错**（故宫善本舊籍早期录入批次，`measures.unit`
  被写死成"冊"，而 `measure_info` 明明是"N卷"，数字本身对，只单位标签错）／
  **46 条不是错**（是我判定法"抓第一个数字+紧邻字"的假阳性，`measures.unit`
  其实已经是对的，别批量误改）／**11 条个案**（8 条同型"篇/冊被标成卷"，1 条
  数字都不同需覆原目，2 条已在 09-08 档挂 `open`，1 条其实也不是错）。
- **找不到原始入库脚本**——翻了 `book-index-draft/scripts/` 全部（含
  `archive/`），没找到把这批 `measures.unit` 写死成"冊"的脚本，判断是一次性
  跑过没留底；同一来源后期批次（带 `catalog: 編號` 注记）已经是对的，说明
  这个录入源已经修过、收尾了，不必再找脚本去改代码，直接修数据即可。
- 修法：938 条用"数字+量词紧邻"法从 `measure_info` 重新抽取（判据现成，全部
  单一量词，无一例外），改写 `measures[0].unit` 这一个键，不碰 `juan_count`／
  `measure_info`；46 条显式排除不动；剩下 9 条个案逐条人工核对原目，不建议
  批量猜。
- 影响面：只碰 `measures`，机械层没有工具读它做业务判断，不触发索引重算；
  判据与 `juan_unit` 回填同源，建议排执行窗口时两件合并成一次全库扫描。

—— juan-unit 道，2026-09-09
