字段命名：`juan_count.unit` 还是 `juan_count.juan_unit`？

第一阶段裁决（任务书 §五·4）字面给的例子是：

> **字段形状取「加进 `juan_count` dict」**：`{"number": 1, "unit": "篇"}`。

键名是 `unit`。第二阶段任务书（05-juan_unit字段与全库回填.md「第二阶段任务书」
§一·1）写的是：

> 写清：字段位置（`juan_count.juan_unit`）……

键名变成了 `juan_unit`。两处不一致，**我按第一阶段的字面例子（`unit`）实作**，
理由是那是唯一给出完整 JSON 例子的一处，`juan_count.juan_unit` 更像是在指代
"juan_unit 这个功能／字段所在的位置"这句话本身的简写，不一定是字面键名。

已落地的三处都用 `unit`：
- `book-index-draft/.claude/skills/hanzhi-curation/scripts/fill_measures.py`：
  `x['juan_count'] = {"number": num, "unit": unit}`
- `book-index-draft/scripts/validate_entries.py`：`juan_count.get("unit")`
- `book-index-draft/SCHEMA.md`：`"unit": "string (optional, 這個數的單位…)"`

**如果协调者的原意其实是键名要叫 `juan_unit`**（比如为了和字段族的名字
`juan_unit` 保持字面一致，或者已经在别处这么用了），**这三处都要改名**，
请明确告知，改起来不费事（都是我自己刚写的，还没有全库回填，改名不涉及
条目数据）。

—— juan-unit 道，2026-09-09
