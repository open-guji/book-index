废juan_count统一到measures：§二两件只读产出已出

任务书新裁决（`进度/B-条目质量/07-废juan_count统一到measures.md`）要求先做
的两件只读事都办完了，**一条数据未改，未动任何脚本、未动前端**。

## 一、引用面普查

结论：**渲染只有一个真正入口**——`book-index-manager`（即 `book-index-ui`
npm 包 ＋ `book-index` CLI 的家）的 `ui/src/core/detail-model.ts` 里的
`measureText()` 函数。但除了这个入口，还有**三条独立通道**同样要改，
逐个列了文件与行号（详见 07 档 §五·1）：

1. `IndexBrowser.tsx`（列表/卡片视图）——**不经过 `measureText()`**，
   自己硬编码 `${entry.juan_count}${t.unit.juan}`，是与详情页平行的第二个
   渲染点，漏改这处列表会跟详情页显示不一致。
2. `book_index_manager/entry_extractor.py` 的 `_extract_juan_count()`——
   **这是 `index/works/*.json` 里那个裸整数 `juan_count` 字段的真正来源**
   （juan-unit 道第一阶段查过 `reindex.py`，没查到，这次在 `book-index`
   CLI 里找到了，不是同一个工具）。
3. `kaiyuanguji-web` 的 `src/lib/search/meili-storage.ts`——**Meilisearch
   索引文档本身带 `juan_count` 字段**，改字段名需要重建搜索索引，不是纯
   代码改动。

另外还有一个意外发现：`overview/scripts/merge_works.py` 合并重复 Work 时，
`juan_count` 走"取较大者"、`measures` 走"并集去重"——**两种策略不一致，
是 342 条分歧至少一部分的成因**（`ai_note` 里能直接搜到"取较大者"的字样）。
`juan_count` 一废，这条逻辑也随之作废。

`book-index-draft`／`overview` 两仓活跃脚本与 skill 各点名列了（validate_entries.py／
fill_measures.py／promote_collection.py／qa_book.py／merge_works.py 等），
归档脚本（各 14／26 个）不逐个列。

## 二、三类边界条目判准

95,354 条 Work 实测：

| | 条数 | 判准 |
|---|---:|---|
| 两者都有，number 相同 | 59,643 | 直接丢 juan_count |
| 两者都有，number 不同・结构性零值（juan_count=0） | 324 | 不是分歧，是既有约定（多单位记录），直接丢 |
| 两者都有，number 不同・**真冲突** | **18** | **唯一需要人判的部分**——判据：谁的数字对得上 measure_info 首个数字就信谁（14 条该信 measures，4 条该信 juan_count），清单见 `overview/scripts/qa/reports/20260909-juan-unit/juan_count与measures真冲突18条.json` |
| 只有 juan_count，number=0/无值 | 312 | 不分卷壳，直接丢 |
| 只有 juan_count，number>0，有 measure_info | 1,076 | 可直接抽取迁移，判据与 05 档 65,628 条同一份代码 |
| 只有 juan_count，number>0，无 measure_info | 37 | 需推，沿用 05 档判据（几乎是同一批 38 条） |

**一句话**：95,354 条里只有 **18 条**真需要人一条条判，其余全部机械可判。

复算脚本 `overview/scripts/qa/reports/20260909-juan-unit/
analyze_juan_count_measures_boundary.py` 已附，跑一遍数字对得上。

## 未做的

执行（真正统一字段、改脚本、改前端）——**没有做**，等 `A-并条` 收工排窗口。

—— juan-unit 道，2026-09-09
