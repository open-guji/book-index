juan_unit 第一步只读调研已交，一条数据未改，方案见 overview 仓任务档

- **道**：juan-unit ｜ **性质**：只读调研，本批未改 schema、未回填、未碰前端、未写任何
  Work／Book／Entity／Collection 条目档、未写 `index/`。
- **方案全文**：`overview/项目进展/古籍索引网站/进度/B-条目质量/05-juan_unit字段与全库回填.md`
  「方案」章节。
- **全量报告与复算脚本**：`overview/scripts/qa/reports/20260909-juan-unit/`
  （`report.md` ＋ `scan_juan_unit.py`）。

## 摘要与计数（全量报告放 reports 目录，这里只留摘要）

用「数字＋量词紧邻」法（非末位法，NFKC 归一化后）全量重扫 `book-index/Work/**/*.json`
95,354 条：

- 有 `juan_count`：60,263；有 `measure_info`：66,261；两者都有：60,225
  （与协调者抽样四个数字完全吻合）。
- **判据分层**：直判 65,628（99.0%，含"卷＋结构性副计量"归并 241 条）／
  无从判 16（0.02%，真两可，如"洪秀全演義＝二集二十九回"）／
  noop 617（0.93%，其中 526 条是"不分卷"，`juan_count` 早已为 0，不需要单位）／
  需推 38（无 `measure_info` 但 `juan_count>0`，其中 26 条其实能从 `indexed_by`
  摘要机械抓到，4 条是跨志书著录量各异的经典，12 条无来源可援引）。
- **枚举建议**：18 个真量词全部照收（卷/冊/篇/回/集/編/種/則/部/章/函/首/筆/期/
  節/帙/弄/件），不设"其他"桶。
- **默认值建议**：能判定的写实值；"不分卷"类不填；真无从判/需推死角（约 145 条，
  **不是任务档原估的 3 万多条**）建议留空不显示单位，不建议保留旧默认"卷"。
- **影响面**：`reindex.py` 的 `expect_work()` **现在完全不投影** `juan_count`／
  `measure_info`；根因脚本是 `book-index-draft/.claude/skills/hanzhi-curation/
  scripts/fill_measures.py` 第97行（不分单位一律塞进 `juan_count.number`）；
  `book-index-ui`／`kaiyuanguji-web` 不在本仓，已列出需去那两仓确认的清单，未猜。
- **意外发现**：`measures[0].unit` 与 `measure_info` 有 995 条不一致，984 条集中
  在"國立故宮博物院善本舊籍"批次（该批 `measures.unit` 被清一色错填成"冊"）——
  `measures` 不能盲信为权威源，本次未修，已记账。
- **与 09-08 合并路径**：`juan_unit` 管单位，`measures[]` 管多值，两者不必合并成
  一个字段；默认值决策同一个道理"宁可留空，不给看起来对的错数"。

## 与任务书/任务档对不上的地方，已写 ask 档

见同目录 `20260909-1900-ask.md`——包括"3 万多条"量级估计偏差、任务档举的"春秋"
无从判例本次未能精确复现、以及本次自己撞见的第五个陷阱（CJK 兼容字）。

—— juan-unit 道，2026-09-09
