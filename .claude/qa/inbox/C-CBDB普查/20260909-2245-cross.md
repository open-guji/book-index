# ⚠️ 裁决表的 117 条分类计数要改：不是 7／4／4，**须解挂的是 55 条不是 15 条**

裁决档 §二把 117 条写成「`auto_create` 前缀误配 **7 例**、异体字之别 **4 例**、明显误配 **4 例**」。
**那 7／4／4 是我 cross 单里的举例数（7+4+4＝15），不是分类总数**——我上一单只举了例、没给每类的数，
是我没写清。C 道若照 15 条去办会漏掉四十条。今补全量分类（仍只读，一条未改）：

## 一、117 条的真实分类计数

| 类 | 条数 | 处置 |
|---|---:|---|
| **a 我方名以 CBDB 名为前缀**（前缀误配） | **51** | **须解挂**。全部 `auto_create`，见 §二 |
| b CBDB 名带尾随空白（`史鑄` → `史鑄　`） | 1 | **CBDB 侧数据脏，非误配**，我方可留 |
| **c 异体字之别** | **24** | **可留，宜补别名**。徐肇臺／台、鄧鍾嶽／岳、呂岩／巖、董金鑑／鑒… |
| **e 仅差一字**（形近／误字） | **27** | **须逐条看**。廖光圖→廖正圖、崔櫓→崔魯、蕭祐→蕭祜、丘悅→丘說… |
| f1 仅括号全／半角之别 | 5 | **同一人，我的判据造的假阳**，见 §三 |
| f2 一方多前后缀 | 1 | 西鄙人 → `釋西鄙人`，多半同一人 |
| f3 名字确实不同 | 8 | 再分两半，见下 |
| | **117** | |

**f3 那 8 条要再分**：

- **名号关系，多半是对的（4 条）**：張志和→`張龜齡`（志和一名龜齡）、
  傅大士→`傅翕`（傅大士即傅翕）、劉希夷→`劉庭芝`（希夷字庭芝）、
  義淨→`張文明`（義淨俗姓張名文明）。**我方用号／法名，CBDB 用本名，不是误配。**
- **疑似真误配（4 条）**：宋汝為→`趙復`、朱亦棟→`朱芹`、張孝嵩→`張嵩`、法雲→`釋灌頂`。

### ⇒ 结算

| | 条数 |
|---|---:|
| **须解挂** | **55**（51 前缀误配 ＋ 4 疑似真误配） |
| 须逐条看 | 27（仅差一字） |
| 可留（宜补别名或原样） | 35（24 异体 ＋ 5 括号 ＋ 1 前后缀 ＋ 4 名号关系 ＋ 1 尾随空白） |

## 二、治本裁决要的证据：前缀误配 **51／51 全部出自 `auto_create`**

按裁决「`auto_create` 这一路的判准要整体复查」，全库扫了一遍
「我方名以 CBDB 名为前缀且 CBDB 名 ≥2 字」的挂接（**不限于那 117 条**——
`linked_name_match` 只看 CBDB 名在不在我方名字列表里，若我方 `alt_names` 里恰好有那个短名就漏网了）：

**全库命中 51 条，`cbdb_match` 分组：`auto_create` 51，其余 0。一条例外都没有。**

明细已落 `overview/scripts/qa/reports/2026-09-09-CBDB普查/附件5-前缀误配全量清单.csv`
（带 entity_id、cbdb_id、双方名字，可直接照着解挂）。样子一律是：

```
何益之 → 388973「何益」    倪立昌 → 27229「倪立」     劉世光 → 390774「劉世」
沈孟操 → 411795「沈孟」    汪顯徳 → 411656「汪顯」    李希雄 → 453497「李希」
許大受 →  95172「許大」    馬時可 → 327425「馬時」    郭茂才 → 540906「郭茂」
```

**51 条全是「我方三字名 → CBDB 两字名」**，CBDB 那些两字名
（`c_surname_chn`＋`c_mingzi_chn` 分解完整、拼音也是两字）**本身就是完整的人**。
判准里只要还允许前缀匹配，这一型就会稳定再生——裁决说得对。

## 三、这一单里我自己的两处判据错，一并报

1. **`linked_name_match` 没做全／半角归一，造出 5 条假阳**：
   楊廣（隋煬帝）／李純（唐憲宗）／武曌（武則天）／李隆基（唐玄宗）／李世民（唐太宗），
   与 CBDB 的 `楊廣(隋煬帝)` 等**只差括号全半角，是同一人**。
   → 报告 §五那句「117 条」里含这 5 条假阳，**实为 112 条真有出入**。

2. **括号混用是我方的事，不是 CBDB 的**：实测 CBDB `c_name_chn`
   **含半角括号 50,852 条、全角括号 0 条——CBDB 一律用半角，很整齐**。
   用全角的是我方那 5 条帝王条目。**差点又把我方的不一致报成 CBDB 的缺陷**
   （这是本道第二次险些报错方向，第一次是「沈孟」截断名那次）。

## 四、顺带给 CBDB 纠错清单补一条（已有实据）

**`c_name_chn` 含空白字符：521 条。** 尾随全角空格如 `39215「史鑄　」`，
括号前多空格如 `5084「胡氏 (呂申妻)」`、`118643「某氏  (嵩山女)」`（两个空格）。
**任何精确匹配都会被它卡住**——我方「史鑄」查 CBDB 就是靠归一化才对上的。
本道的 `norm()` 用 `"".join(s.split())`（Python 的 `split()` 认 U+3000），已吃掉这一型。

> 这一条**是 CBDB 侧的数据脏，可自证**（`select c_name_chn from BIOG_MAIN where c_name_chn <> trim(c_name_chn)` 之类即可），
> 已并入附件 4 的类型⑤「姓名残缺／占位未回填」。
