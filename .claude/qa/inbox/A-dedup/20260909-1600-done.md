A-dedup 第三批：收工

做了什么：乙案落地（`works_clean` 建库端与查询端改用同一函式
`norm_shared.clean_key()`，坑 88 治本，撤查询端两式并集补偿）；同志内部重出
13 对三类处置办妥。**这一批改了既有代码，限于点名的四个文件**——已自证
`git diff` 范围（见批三 commit）。

数字：
- 三条回归判据全过：法七 **584 对**（第一批回归集）全中 0 缺；净题 **59 条**
  （第二批实测漏检）全捞到；除此之外命中集合**净增 174、净减 0**。
- 净增 174 逐一核过非误并——增量全是同净题桶下彼此原本不可见之候选（因原文
  变体写法各据一桶而合并所致），性质与既有法一致，是查全率提升。
- 同志内部重出 13 对：甲·志文「又」续记体 3 对落 `normal`；乙·撰人黏题首
  同志两批各录 6 对入待并清单（与第一批 444 对合并为 450 对）；丙·记录内部
  撰人歧异待人辨 4 对写待审档。

依据：任务书第三批之乙案裁决与四条回归判据；同志重出三类处置依协调者所定。

验数：净题 59 条核对时撞见 `dedup_check.py` 输出层 `[:8]` 截断致假警报——
已改用未截断之底层 `hits` 集合核实，非数据缺陷，只是显示层限制，记入报告
供参考（未改动，不在本批点名文件范围内）。

待裁：丙类 4 对写了待审档（`overview` 之待审元数据
`A-dedup-20260909-同志内部重出丙类4对.md`），未停下等回音。

负结果：无——三条回归判据全过，未撞见需要停下重报的新缺陷。

下一道要知道的：
- `works_clean` 的键正规化口径现已写在 `build_match_index.py` 顶部与 pkl
  之 `_meta` 字段，日后任何要查 `works_clean` 的新脚本都呼叫
  `norm_shared.clean_key()`，不要自行展开。
- `dedup_check.py` 输出对每条 `hits` 有 `[:8]` 截断，候选逾 8 个时后面的会被
  裁掉看不见（底层其实找到了）——若日后要精确核对某条候选是否被找到，
  不能只看输出 JSON，要看底层 `hits` 字典。
- 待并清单（450 对）与丙类 4 对待审档，是本道两批以来产出的全部"报出候选"，
  并条与丙类之数据质量问题都不在本道写域，交后续道处置。

本道三批全部收工。**一条数据都没改**（待并清单是候选清单，不是并条动作；
待审档只是记录）。
