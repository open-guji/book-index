A-dedup：收工

做了什么：`dedup_check.py` 加第七法「剥题首之撰人」；库内自检回归集 584 对，
两证分类出报告；`match_index.pkl` 出仓。

数字：法七回归集 584 对（任务书原估 558，出入见 ask 档），100% 命中、0 缺。
两证分类：俱全 444 ／缺一 104 ／皆无 36（分母皆 584）。另分出「同志内部重出」
13 对、「卷数异之候选」36 对两类次生问题（见 check 档）。

依据：任务书 §二·1 之判准（剥 2–4 字前缀＋核对候选撰人）；两证判准同 §二·2
（著录集合两两不相交／两造都有卷数者卷数相同，卷数比对复用 `scan.py` 之
`juan_of()`，PITFALLS 47 同一把尺）。

验数：法七加入后跑 `dedup_check.py` 对 584 对合成 entries.json 全部命中，
0 缺（含任务书点名 5 组抽验样例）。`match_index.pkl` 从零重建核实无碍
（`BOOK_INDEX=... python3 build_match_index.py` 正常出档，计数与库内现状一致）。

待裁：ask 档（数字出入）、check 档（两类次生问题）已写，均未停下等回音，
按工作流写完即继续，现已收工。

负结果：`works_clean` 查询套 `norm()` 而键未经正规化，第一版法七因此漏了
54/549 条实测应中之对——已修（原文、正规化后两式都试），写进 pit 档，
并提醒既有「净题」一路可能同患（约 336 条潜在暴露面，未逐条验证）。

下一道要知道的：
- 坑：pit 档所记 `works_clean` 键未正规化一事。
- 常态：法七只对撰人栏本空者试，不覆盖撰人栏已有值之条目。
- 可复用脚本：`scripts/通用/scan_prefix_dedup.py`（全库自检，不改数据，可重跑）。
- 遗留：「同志内部重出」13 对成因未查（同批录入拆出两条？志文本身重出？）；
  「名未归一而撞不上」未新增专项扫描，沿用交底给的《甲寅元曆序》一例说明边界。

报告全文：`overview` 之 `项目进展/古籍索引网站/目录录入/二十五史艺文志考补/
reports/20260909-撰人黏题首/报告.md`
