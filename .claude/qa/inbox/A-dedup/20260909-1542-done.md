A-dedup 第二批：收工

做了什么：净题一路（坑 88）实测漏检数、甲乙两案修法方案、「同志内部重出」13 对
逐对查因分类、「卷数异」36 对落账。**仍未改既有代码、未并任何条目**。

数字：
- 净题一路实测净漏检 **59 条**（涉及 26 个不同题名；粗估口径原是暴露面 336，
  实测扣掉已被其他六法捞到的部分后，真正独有的净新增只有 59）。
- 全库查 `works_clean` 只 `dedup_check.py` 净题段这一处未修（法七、
  `scan_prefix_dedup.py` 已修，其余脚本只用 `entities`，不涉 `works_clean`）。
- 甲案（改查询，1 处）／乙案（改建库，需同步换手，重建实测 15.2 秒）两案并陈。
- 「同志内部重出」13 对：甲·志文「又」续记体 3、乙·撰人黏题首同志两批各录 6、
  丙·记录内部撰人歧异待人辨 4。
- 「卷数异」36 对已落 `verdict=normal`，`rule="I-卷數異即異書"`，
  `by="A-dedup"`（`book-index` commit `f3125194da`）。

依据：任务书第二批 §1–3 之判准；卷数异落账依 PITFALLS 47。

验数：净漏检测量用「镜像 dedup_check.py 全七法、只切换净题 WC 查询」之办法，
比对整条命中 wid 集合（不分 kind）之差，非单纯 WC 层面之差集——**这一步是本批
方法论上最要紧的一处**，粗估 336 到实测 59 的落差正来自「精確等其他六法已经
捞到大半」。

待裁：甲乙两案未强行推荐一条，写清代价交协调者；「同志内部重出」丙类 4 对
需人工细究，未处置。均未停下等回音。

负结果：无——本批测量与分类顺利，无踩新坑（坑 88 已在第一批记过）。

下一道要知道的：
- `works_clean` 建库端（`build_match_index.py` 之 `cmap`）与查询端只有
  `dedup_check.py` 净题段这一处口径不一致，改一处即可全愈（甲案）。
- 「同志内部重出」丙类 4 对（史蘇龜經／龜經、徐陵六代詩集鈔／候选、
  杜乂甄正論／候选、謝良佐論語解／候选）疑涉既有頂真格解析或跨志异说，
  非本道能自动判定，留给熟悉頂真格解析（宋志／新唐志抽撰人）的道核校。
- 全部数据与方法见报告：`overview` 之 `项目进展/古籍索引网站/目录录入/
  二十五史艺文志考补/reports/20260909-撰人黏题首/报告-第二批.md`

本道两批皆已收工。
