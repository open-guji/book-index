A-dedup：works_clean 之键未经变体正规化，查询套 norm() 会静默漏命中

`match_index.pkl` 之 `works_clean`（简称 WC，题名净题 → 候选）建库时（`build_match_index.py`）
键直接取 `clean_title(t)` 原文，**未经**变体表（`norm_var`）正规化；而 `dedup_check.py`
既有之「净题」一路查 WC 时写的是 `WC.get(norm(ct or t), [])`——查询套了 norm()，键却是
原文，两边不一致本该常常落空，只是这批命中的净题大多恰好不含变体字才没露馅。

本道加法七时踩实：候选剩余部分「兩京新記」被 `norm_var()`（题名补表，記→紀）转成
「兩京新紀」，`WC` 里只有原文「兩京新記」那个键，查询直接落空——回归集 549 条中
54 条本该中却测不中，全部拜此所赐。

已修（限法七这一段，未动既有「净题」代码——不在本道写域，未审既有行为改了会不会
牵动别处）：查 WC 时原文、`norm()` 正规化后两式都试，取并集。修完，回归集全数找回。

**这一条同样潜伏在既有「净题」一路**（`dedup_check.py` 行 131 附近：
`for c in WC.get(norm(ct or t), []): ...`）——粗估：净题（`ct != t`）且
`norm(ct)` 与 `ct` 不同（即含变体字，正规化后确实变了形）、而 `ct` 原文本身在
`WC` 里的条目，全库约 **336** 条，这些若真被撞库都会因同一根因而漏检。
本道未逐条验证净题一路实测漏了多少（那是既有代码，不在本道写域），只给这个
粗估口径，供下一道或协调者按需查证、决定要不要一并修。
