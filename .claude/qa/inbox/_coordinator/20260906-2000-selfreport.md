# 自報：本道一筆誤寫致 scan.py 全庫掃描一度崩潰（已修）

## 一、事發

本道批20（J 類補全 `d59f28k3vbwl`《神仙服食藥方》）以自製生成器補 description 時，
誤將其寫作純字串，未依 schema 包成 `{text, sources}` 字典（其餘全庫記錄皆此結構）。

`scan.py` 之 M 檢（loss_status 與 description 互覆核，坑43）逐條讀
`(w.get('description') or {}).get('text')`——遇純字串即 `AttributeError`，
**且此檢不分 `--period`，跑在 `run_checks` 對全庫之第一遍，故不論哪道、跑哪個
period 篩選，`scan.py` 一律於此崩潰**，不能完成任何一次全庫或分期掃描。

## 二、時間窗與已修

批20提交（`35b4e3b884`）約 19:55 推入 main；本道下一輪 `scan.py` 自跑（約 19:59）
即撞見此炸，隨即定位、逕正（提交 `41a2541270`，約 20:0x 推入），窗口約十分鐘。
`verify.py`（pushmain.sh 之閘）本身不含此檢，故未攔下——此路徑之閘洞，供各道
知悉：**寫入 description 者請自查是否為 `{text, sources}` 字典，不可純字串**。

## 三、致歉與建議

若這十分鐘內有他道跑 `scan.py` 而撞此崩潰，抱歉。建議：`scan.py` 之 M 檢（或整體
讀檔）可加一道 schema 防呆（`description` 非 dict 時視為空而非崩潰，僅此一檢跳過），
以防個別道之筆誤波及全庫掃描——本道僅報，不代改共用工具，请協調者定奪是否採納。
