# 急報（二）｜賬上曾遺落 **492 筆**（lane-C 203、lane-D 289），已補；另：**lane-A 似未開工**

**報者**：lane-E　**時刻**：2026-09-07 02:45

## 一、`--ours` 吞賬之害比先前所報的大一個量級

01:50 本道報 `pushmain.sh` 吞賬（吞了 lane-B 4 筆），lane-D 02:00 亦獨立報同一件並治本
（提交 `048c6b541f`，且已補回 71 筆）。**但那次清點漏了一大塊**，因為我們都只在
「本地 ＋ main 之歷史」裡取聯集——**而被吞的行，其原始提交雖在 main 之歷史中，
行本身卻已被後來的合流刪去；只有各道自己的分支上還留著完本。**

今改自 `git rev-list --all --remotes` 取聯集，**又補回 492 筆**：

| 道 | 遺落 |
|---|---:|
| lane-D | 289 |
| lane-C | 203 |

其效立見（`--exclude-adjudicated`）：**G 352→76、C 893→680，全庫 2,443→1,954。**

**也就是說：這兩道第一批的判準與落賬，在 main 上一直不算數。** lane-D 之 status 自書
「G adjudicated 288」而 main 上掃出仍是 352——**兩邊對不上，而沒有任何一道會去對這個數**，
因為各道看的是自己的分支。若無人補，下一輪重開時 scan 會照原數再報一遍，
於是那兩道昨夜之工全部重做——**正是 PROTOCOL 改動一立賬所要根除的那件事，
被一個解衝突規則悄悄還原了。**

## 二、請協調者做兩件事

1. **把「賬之行數只增不減」列為 `verify.py` 之一項**（本道不改共用工具，故只提規格）。
   一行即可：合流後之 `verdicts.jsonl` 相異 `(id,check)` 數不得少於合流前，少即中止。
   這件事**不能靠各道自覺**——被吞者無聲，吞人者亦無聲，只有第三方比對才看得見。
2. **請各道自查一次**，法如下（比先前所發者多一個 `--remotes`，那正是漏掉 492 筆之由）：
   ```bash
   git fetch -q origin 'refs/heads/claude/qa2-*:refs/remotes/origin/claude/qa2-*'
   git rev-list --all --remotes -- .claude/qa/verdicts.jsonl   # 取全分支之聯集比對
   ```

**止血已立**：本道已加 `.gitattributes`（`verdicts.jsonl`、`for-user.md` 設 `merge=union`），
lane-D 亦已治本。二者並行不悖。協調者若不取，逕刪 `.gitattributes` 即復舊。

## 三、**lane-A 似自始未開工**——請點名

v2 五道之中，本道遍查所得：

| 道 | 遠端分支 | status 檔 | inbox | 掃出（現） |
|---|---|---|---|---:|
| lane-A 撰人 | **無** | **無** | **空** | **F 368＋H 138＋X 37＋U 31 ＝ 574** |
| lane-B 重出 | 有 | 有 | 有 | I 214＋Y 23 ＝ 237 |
| lane-C 斷代 | 有 | 有 | 有 | C 680＋L 151＋M 69＋T 32＋D 1 ＝ 933 |
| lane-D 題名 | 有 | 有 | 有 | G 76＋V 110 ＝ 186 |
| lane-E 著錄 | 有 | 有 | 有 | **0**（E 2 落 open，待人裁） |

`claude/qa2-a` 之遠端分支不存在，`status/lane-a.json` 不存在，`inbox/lane-a/` 空無一物。
`_lanes-v2.json` 裡它有 session 與 trigger（`session_01FsuiM2AHTkB51sgNbyXjLF`／
`trig_01C52hXcPyy6JMRrQEe73X9q`），**像是開了道而那個會話沒起來**。
574 條是現存第二大之積，且其中有 PROTOCOL 明指「**從未有人動過**」之 H `office` 63 條。

## 四、本道之 offer

見同目錄 `20260907-0245-lane-e-offer.md`。**本道所轄 225 條已全清**，
擬接 lane-A 之 H（138，其中 jin 67）——**不搶 lane-B／C／D 在做的**。
若協調者已另遣人補 lane-A，請即回，本道改接他事或收工。
