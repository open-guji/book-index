先秦-條目收尾道收工：任务书 §二 五类账逐处有结论，一处不留（改了／裁为不改／转待审档）。

## 做了什么

点名清账，§二五类逐处过手：

- **二·一(a)** 三处漢志 summary 录入误（丁氏「子襄→元襄」、公孫固「十八章→十八年」、
  黃帝君臣「起六國也→時」），据 A-漢志補錄道所验影印本页码逐条复核后改。
- **二·一(b)** 龐煖「三篇→二篇」，改 title_info／summary／measure_info／
  measures[0].number／juan_count.number 五处，据同页「大夫種」有校记而本条无校记
  之法判为录入误。
- **二·一(c)** 徐氏→徐樂：核实库中早已办竣，未改动（详见 `ask` 单）。
- **二·一(d)** 鄧析子简体字混入已改；顺手用「NFKC→opencc s2t 全表→去标点」三层
  归一法扫全先秦 342 条 `indexed_by[漢書藝文志].summary`，另找出 4 处真issue
  （文子「与／并→與／並」、尉繚子「缭→繚」、呂氏春秋「辑→輯」、晏子春秋「与→與」，
  皆有同页 self_note 佐证）已改；關尹子「从」按既有明确先例（A-漢志補錄道已判定
  「从／從」为传世异体、非简繁之别）**未改**。另扫出 `indexed_by[].text`
  字段（非 summary）大面积简体污染，不在写域，未动，详见 `ask` 单。
- **二·一(e)**「大禽二十七篇」核实系「大命二十七篇」之转录笔误（页码、篇数全同），
  并入 (f) 处理，未另立。
- **二·一(f)** 新建 Work 2 条（大命 `d59fdyfp4f0g`、鵄冶子 `d59fdyfp4f0h`，皆据
  影印本 vol01.pdf p042／p053 核实），登记进 Collection/8rld0tpplp8g（按漢志六略
  原序插入）；孔甲盤盂（`d59f2q7bhxc1`）库中早已建 Work，只补登记进 Collection
  与 `contained_in`；常侍郎莊忽奇賦（`d59f23npjguf`）库中早已建妥，顺带发现并订正
  title 一族「莊匆奇→莊忽奇」之误植（据影印本核实）。三条建条前均已撞库查重。
  Collection total_works 199→202。
- **二·一(g)** 揚雄所序结构缺口：写待审档报协调者，未擅自建条目。
- **二·二** 10 处 `authors[].entity_id`：先 grep 撞了 C-entity 併条史，孔子／陸希聲
  繫既有 Entity 并双向补链；周公／虞卿库中查无现成 Entity（与任务书预期不符），
  各新建；尉繚繫既有 Entity 并双向补链；孔門後學／李氏／野老三处泛称落账说明不建
  Entity；王史氏一处**未繫**——该 Entity 有 2026-09-05 明确解繫先例，判其为泛称，
  与任务书预期相反，尊重既有裁决（详见 `ask` 单）。
- **二·三(a)** 7 处题名不符：6 处（周易／詩經／周禮／左傳／論語／孟子，孟子那条内
  2 处）以目标 Work 现行 title 回填；李子→季子 1 处读过待审档后未擅改，仅补一句
  发现进待审档。
- **二·三(b)** 8 组单向关系（19 条边）：先查 SCHEMA 关系词表，`text_carried_by`
  之反向为 `contains_text_of`、`studied_by` 之反向为 `studies`（4 条属此类，
  任务书笼统称"全是 text_carried_by"不够准，详见 `ask` 单）；19 条反向边全部补齐。
- **二·四** 偽古文尚書、關尹子宋託名本各补 `description.sources`（据 description.text
  内已引用的书目），未动 `text`，未含溫浚源《漢書藝文志講要》。
- **二·五** 馮促／周伯／神農雜子技道 3 条核实为假阳性（09 档 §五「最短一路」样板），
  未改，已落此单——建议 `audit_classic.py` 的字数下限对「惟一志著录、无可考」这类
  放宽。

## 验

```
grep -rl '^<<<<<<<' Work Entity Collection index   # 0
python3 -c "全库 json.load"                         # 坏档 0
python3 .claude/qa/reindex.py --run --membership    # 补键4（2 Work+2 Entity）、修path 1
python3 .claude/qa/verify.py --strict               # OK（漂移0/懸空0/单向0）
```

`audit_classic.py` 重跑 208 条（原 205 条 + 本道新登记 3 条，分母已变，见下）对比：

| 项 | 开工基线（205条） | 收工（208条） | 说明 |
|---|---:|---:|---|
| 題名與目標不符 | 7 | 1 | 6 处已回填，余 1（李子/季子）待审档中，非机械问题 |
| 單向關係 | 8 | 0 | 19 条反向边全部补齐 |
| description 無 sources | 2 | 1 | 点名 2 条已补；新增的 1 条是本道登记盤盂入 Collection 后新入范围的既存缺口（非点名对象，见 ask 单） |
| description 過簡 | 3 | 3 | 确认假阳性，未改（按判据不应改） |
| loss_status 空缺 | 5 | 5 | 确认非缺口（SCHEMA 明文），未改 |
| 斷代語宜互核 | 8 | 8 | 不在本道写域，未动 |
| 版本-無edition | 3 | 3 | 不在本道写域，未动 |
| 無任何Book | 143 | 146 | 分母 205→208，新增 3 条（大命/鵄冶子/盤盂）皆无 Book，非本道写域 |

## 待裁 / 负结果 / 下一道要知道的

- 待审档 2 份：`overview/.../待审元数据/先秦-條目收尾-20260914-揚雄所序占位條目.md`
  （新，待协调者裁）；`先秦典籍-20260909-季子题名与汉志著录相牴.md`（补记一句）。
- `ask` 单：`20260914-1200-ask.md`，7 条任务书与实测不符、4 条域外顺带发现，
  详情不重复列，见该单。
- 负结果：試過但判定不改的——關尹子「从」（既有先例）、馮促/周伯/神農雜子技道
  「過簡」（假阳性）、李子/季子题名（候人裁）、王史氏 entity（既有解繫先例）。
- 下一道可复用：全先秦 342 条简体字三层归一扫描脚本（本单未附档，逻辑见上），
  `indexed_by[].text` 字段的简体污染是现成的下一批活。

## 收工时状态

写域内全部收工，`verify.py --strict` OK，已合并 origin/main（见下），
拟 `pushmain.sh` 推送。
