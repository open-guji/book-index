# 請加一項檢查：單向邊（entity.works 有而 work.authors 無回指）

`verify.py` 現查四項：索引漂移 ×2、work 側懸空、entity.works 懸空。**不查單向邊**——
邊兩端的記錄都在，只是一頭指得出去、另一頭指不回來。上一則 pit 所記的四例正是
這樣漏過去的：懸空是 0，索引漂移是 0，verify 全綠，而庫裡躺著四條假繫連。

## 掃法

```python
back = {}                      # work_id -> {entity_id}
for w in all_works:
    back[w['id']] = {a.get('entity_id') for a in (w.get('authors') or []) if a.get('entity_id')}
oneway = [(e['id'], wid) for e in all_entities
          for wid in {x['work_id'] for x in (e.get('works') or [])}
          if wid in back and e['id'] not in back[wid]]
```

反向也該查（work.authors 指人而該人 works 未載），但那一支 coordinator 的
「人物↔作品雙向鏈補全」已經補過 6,366 條，眼下應是 0；加進去做回歸即可。

## 基線

全庫現有 9 條，是既存的，不是本輪引入。明細：周弘正→《論語疏》、徐子→
《平徐氏珞琭子賦注》、周生→《烈要論》、儀曹→《陳皇太后崩儀注》、郭璞→
《山海經》《穆天子傳》、甄叔遵→《七曜本起》、汪晫→《曾子》、楊休之→《辯嫌音》。
前六條 work 側 authors 為空（撰人未錄），後三條 work 側繫了別人。皆待逐條裁。

`overview/scripts/cbdb-sync/verify_repo.py` 裡有現成實現（`PREEXISTING` 記著基線），
可搬。
