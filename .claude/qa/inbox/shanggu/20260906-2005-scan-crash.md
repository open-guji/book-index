# 急件：scan.py 曾全庫崩潰（約 19:59–20:05），已修，可以跑了（協調者之過，坑 54）

## 一、怎麼回事

M 類那行寫作 `((w.get('description') or {}).get('text') or '')`，**假定 `description`
必是 dict 或 None**。而庫中確有存**純字串**者（`d59f28k3vbwl`，weijin 桶），
`.get` 施於 `str` 即 `AttributeError`——**全庫任何 `--period` 皆崩**，九道一時盡不能掃。
nanbeichao 道七分鐘內報到並指出行號與修法。

**已修**：另立 `desc_text(w)`，容 dict／str／None 三型。**取容型一路而非改那一條資料**——
資料之型本有歷史，改一條治不了下一條；**掃描器是讀者，讀者當容作者之異**。
（那條 description 仍宜正規化為 `{"text": …}`，已轉 weijin。）

## 二、你要做的

`git fetch origin main && git merge origin/main`，然後照常掃。**崩潰期間跑不出來的批次重跑即可**，
無資料損傷。

## 三、與此相連的另一件（同一小時內）

`scan.py` 之 **G 類判準曾被我覆寫**（坑 53，19:12–19:55），該窗內 G 之輸出全部失真——
合法題名（《梁武帝集》《陳後主集》）被誤判為殘語，真殘題反倒消失。**若你在該窗內依 G 作過判，
請重跑覆核，尤其 `clash` 一路**（clash 是在 G 之候選集上算的）。詳見 `20260906-1955-G-bug.md`。

## 四、兩條通則（坑 54）

1. **掃描器讀任何欄，先問「這欄在庫中有幾種型」。** 已知型不一者：`description`（dict／str）、
   `alt_names`（dict／str）、`merged_in`（dict／str）、`indexed_by[].author_info`（有無皆可）。
   **一個 `AttributeError` 就能讓九道全停——掃描器之健壯，比它多掃出幾條更要緊。**
2. **自檢要走各道實際走的路。** 我立坑 53 時已自訂「改工具後先重掃比對前後之數」，
   卻**只做了全庫掃、沒跑 `--period`**——而崩的正是 `--period`，各道日常所用之路徑。

## 五、協調者之自省

今日我改共用工具而出事已**四次**：坑 45（Y dup 誤判同經異譯）、坑 48（X 之小注判準方向反了）、
坑 53（覆寫 G 之 `TITLE_RE`）、坑 54（此則）。**四次皆由車道抓出，非我自檢所得。**
規程第 3、6 條已補入自檢步驟。**各道遇工具異狀請照舊速報——這條路是有效的，
今日兩次全庫性事故都是七分鐘到四十分鐘內就被你們攔下的。**

## 六、順帶一則好消息

重掃後 **M 由 1,632 驟降至 78**——依「數目突變先疑判準」查過，是 **song 道補 loss_status
1,320 條並收工**所致，非檢之退化（song 桶 desc 稱今存者 1,327，其中 1,320 已填，逐一驗過）。
**song 道已報收工。** 現九道中 liaojinyuan／shanggu／suitang／song 四道報 done。
