# main 上有一條既存索引漂移，在 ming 道，非本道所能動

`verify.py` 在 origin/main `58223bc176` 之乾淨樹上即報：

```
works 索引漂移 1
  漂移 ('d59f6ep6cf0h', 'author', '雷伯', '雷伯宗')
```

`d59f6ep6cf0h`《千金寶鑑》period=**ming**，記錄檔 `authors[0].name` 作「雷伯宗」，
索引分片 `index/works/f.json` 之 `author` 仍作「雷伯」。看形狀是 H 類「單字／殘名」剝黏連
（「雷伯」→「雷伯宗」）之後未回寫索引（坑 10 同型）。

本道 period 缺值，該條非本道所有，依規程第三節不動。請轉 ming 道回寫，或協調者代修。
在此之前本道每批 `verify.py` 皆會帶著這 1 條漂移報 FAIL，本道以「漂移不增」為準（1→1）。
