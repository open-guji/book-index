# A-考补卷4 → 协调者：`calibrate.py` 要求与本道实测路径对不上，不阻塞，按视觉识别继续

## 症狀

接续令 §四要求"版面参数必须用 `calibrate.py` 标定，绝不许照抄汉志的数（字距、
标点挤压、大小字阈值全是一卷一量；人手填错 DP 会整段错位而不报错）"。

**实测**：`calibrate.py` 在 overview／book-text／zhengshi-yiwenzhi 三仓均不存在，
它是 `05-二十五史考补全面改新格式.md` §七与 `任务书-E-汉志忠实md.md` 里**设计中**
的脚本，服务对象是**本地 PaddleOCR 坐标输出 + DP 自动分行/分级算法**那条链路
（E-汉志忠实md 道 `整理本/漢書藝文志/scripts/build.py`/`segline.py` 一套）。

本道云端会话走的是 `paddle-ocr-structured` skill 指定的替代路径：渲染150dpi PNG
→ 视觉识别 → 人眼判正文/夹注 → 组装md，**没有 DP 自动分行算法，也就没有"字距/
标点挤压比/大小字阈值"这类需要数值标定的对象**——大小字是肉眼逐页判的。

## 已做

不阻塞，按已验证有效的视觉识别流程继续做（本轮新增书页15-16，闸差0，
详见 `目录录入/二十五史艺文志考补/reports/20260910-第4卷/07-接续令执行记录.md`
§五）。

## 请协调者裁

1. 若本意是"视觉识别路径不需要 calibrate.py"——本道按现状继续，无需回应。
2. 若本意是"即便视觉识别也要留一份可复核的量化记录"（比如量出本卷大字/小字
   的实际磅值或行高比，写进一份`juan04.yml`配置档，供以后换回本地 PaddleOCR
   链路时复用）——这个可以做，但形状不是"标定脚本"，需要协调者明确要不要做、
   做成什么样的产物再排期，本道不会自行猜一个形状出来做。
