# lane-D → 協調者：G 之成員迴圈未排除墓碑（判準之修，本道不改 scan.py）

## 病

`scan.py:294`
```python
if TITLE_RE.search(w.get('title') or ''): R['G'].append(row(w, clean=clean_title(w.get('title') or '')))
```
此迴圈**不跳過 `merged_into`**。而其後補 clash 之處（`scan.py:368-376`）**跳過**：
```python
for w in works.values():
    if w.get('merged_into'): continue      # ← 坑 37 所修者在此
    by_title[(w.get('title') or '').strip()].append(w['id'])
```

坑 37 修的是**候選池**（被撞的一方），未修**報出的一方**。

## 量

本輪 G 357 條中，**52 條是墓碑**（15%）；其中 **44 條落在 `clash=true`**，
佔 `clash=true` 112 條之 **39%**。44 條裡 **41 條**之 `clash_ids` 恰是其自身之 `merged_into`
——即「已併之條撞上自己併進去的那一條」，每輪重報一次。

坑 37 記「全庫 clash 207 → 191」，看似已降；實則降的是被撞的一方，
報出的一方仍在。**一頭已修、一頭未修**，正是坑 42 之「只做一頭比兩頭都不做更難查」。

## 擬修

```python
if not w.get('merged_into') and TITLE_RE.search(w.get('title') or ''):
    R['G'].append(row(w, clean=clean_title(w.get('title') or '')))
```

**改前請先 `grep` 一遍**（坑 53）：`TITLE_RE` 在本檔僅此一處用於 G，
另有 `TITLE_RE_IMPERIAL`（136 行）名近而實異，勿動。

**改後請跑 `--period` 比對前後之數**（坑 54、坑 53 通則 3）。預期：
G 全庫 357 → 305；`clash=true` 112 → 68；`clash=false` 245 → 237。
`none` 桶之 203 應降至約 190。**若降幅與此差得離譜，先疑改法。**

## 不必等此修

本道已把該 52 條落 `G-墓碑` `normal`，`--exclude-adjudicated` 自此不再報，
故**本道不因此停**。提此案是為免他道、後輪重蹈，且此病同型或見於他檢
——**凡「報出之一方」與「被撞之一方」分開算的檢，都該問一句墓碑算不算活條**。
本輪所見：`I`、`Y` 之分組迴圈皆已 `continue` 墓碑（`scan.py:378-384`），
`H`／`F` 之成員迴圈與 G 同在一個 `for w in works.values()` 內，**同樣未排除墓碑**
——請一併量一量（本道未查其數，不敢代斷）。
