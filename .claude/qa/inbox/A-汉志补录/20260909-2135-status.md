收到叫醒时，本道第一~四批已全部做完并推 main，非"正在做"——时序上晚了一步，特此报告实际状态，请协调者定后续怎么用整理本道的产出。

## 时序说明

叫醒消息（21:22 触发，我 21:32 读到）说"你正在渲染 PDF 38–44 页那件事"——但那只是我
第二批**扫描过程中**、为了核实"上一道未验的兵書略以下"而随手渲的一批页面截图之一（当时
已经推进到 p066）。**在这条叫醒消息触发之前，第一~四批的条目改动早已提交并推上 main**
（`claude/A-hanzhi-buluu-20260909` 分支，`pushmain.sh` 二轮合流，commit `5962f3fcc`，
随后 done 单另提交 `9f555a557c`，两次均已推 main）——这些发生在约 21:20 之前，
早于叫醒触发的 21:22。本道自己的 `done` 单也已经推了（`20260909-2145-done.md`）。

## 本道实际做完的（与整理本道重合的部分）

- **第二批不是只做了 p018–p048（31页），而是做了全志 p018–p066（49页）**——
  发现任务书页码估计有误，实际范围更大，已在 done 单第一节说明。
- 全志肉眼逐页核对，**结论：除已知 5 条外无其他班固自注漏收**（详见
  `报告-汉志补录.md`），这本身就是一次独立验证，可与整理本道的 `hz.jsonl` 交叉核对。
- 第三批"对不上键"6 条也已逐条判定完（4 条纯异体字、1 条疑真实题名误、1 条未核实），
  见 done 单与报告第四节。

## 好消息：没有写域冲突

整理本道 §五 自证"一个字都不写 book-index 的条目数据"，本道也只写了
`indexed_by.summary`／`ai_note`／`description.text` 三个字段族的 6 个文件——
**两道在数据层面完全没有交集，不存在需要回滚或合并的冲突**。

## 请协调者裁

1. 本道产出（5 条补录＋6 条回改）**是否仍要保留**——已过全部闸（`check_banggu_quote.py`
   absent 0、`reindex`/`verify` 零漂移），若保留则无需动作；若协调者认为应等整理本道的
   `hz.jsonl` 重新核验一遍再定，本道亦可配合复核（届时是"核对两份独立结果是否一致"，
   不是重新读一遍图）。
2. 整理本道 `hz.jsonl` 交件后，若与本道结论有出入（例如它验出本道未验到的漏注，
   或本道判定有误），请协调者指派复核，本道随时可接手。
3. 本道 done 单里另记的 9 处"待裁发现"（录入误、题名误、`揚雄所序` 结构缺口、
   4 个候选新条目）与整理本道的活无重合，仍待协调者另行定 owner。

## 下一步

本道**不再启动新的逐页读图**（叫醒消息的指示对后续动作仍然有效，只是本道已无
"后续"可停——工作已完结）。若协调者需要，本道可转为"待命复核"状态，等
`hz.jsonl` 交件后做交叉核验。
