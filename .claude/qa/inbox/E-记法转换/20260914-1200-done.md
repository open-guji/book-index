任务书四条判据全部办竣：转换器同认 ⟨…⟩ 与（（…))、补齐 .json 支、汉志回归验证通过、报告已出。

## 办了什么

1. **`.json` 支补上**（原来只有 `.md`）。目标结构 `{title, sections:[{title,level,type,
   content,work_id}]}`，只改字符串字段里的记号，键与结构一字不动；写盘前先核
   `JSON.stringify(原对象,null,2)` 与原文件字节级往返相等（容许尾换行有无这一处差异），
   对不上就整档跳过。
2. **加认 `（（…))`**（全角双括号，第四种夹注记号，与 spec 的 `bz` 半角 `((…))` 无关，
   纯属字面撞形），无条件转，同 `⟨…⟩`。
3. **汉志新本回归验证**：取转换前版本（`git show a7b9e50~1`）重新跑新代码，输出与
   协调者已推 main 的 `汉书艺文志.md` **逐字节相同**（`diff` 无输出）——新代码没改坏先例。
4. **转换报告已出**，逐档列应转/实转/跳过（含原因），跳过均可数可列举。
5. **顺手办**：`转记法.mjs` 路径可配置（`--核` 参数或 `GUJI_MARKDOWN_CORE` 环境变量），
   不再硬编码 `/home/user/guji-markdown/...`。
6. **顺手修的一个隐患**：原脚本对配不成对的孤立起止符是**静默丢弃、不进任何报告**的。
   本道把它变成「起讫不全（孤开/孤闭）」这一类新的可数跳过。

## 在《宋史·藝文志》`d59f2gdp696q` 上验（book-text 全程只读，输出到临时目录未写回）

| 支 | 扫档 | 改档 | 处数守恒 | 逐字等同 | 解析回归 | 降级 | 跳过：起讫不全 |
|---|---|---|---|---|---|---|---|
| md | 44 | 43 | 1,418→1,418 | ✓ | ✓ | 0 | 孤开23／孤闭23 |
| json | 44 | 43 | 986→986 | ✓ | ✓ | 0 | 孤开1／孤闭1 |

三组输出各自复跑一遍均报「应转 0」，幂等验证通过。`book-text`／`guji-markdown` 收工时
`git status` 均干净。另做了一次全仓 `--干跑`（不写盘，仅验证不崩）：4,941 档，
处数守恒 21,566→21,566，三道闸全过，0 降级——**这只是顺手验证转换器在全库不崩、
账能对平，不代表本道已做全库真转**（真转按任务书 §六仍不碰，等 UI 道配对上线）。

## 写域内产出

- `项目进展/古籍索引网站/目录录入/二十五史艺文志考补/scripts/格式对齐/转记法.mjs`（改）
- `项目进展/古籍索引网站/目录录入/二十五史艺文志考补/reports/20260914-记法转换/`（新增，
  含 README.md 与三份 report-*.json）
- 任务书末尾「收工时状态」已写

## 与任务书不符之处

已写 `ask` 单（同目录 `20260914-1200-ask.md`）：08 档记 `（（…))` 全库 1,423 处，
本道实测原始子串数 1,441（同段可配对 1,418 ＋ 跨段两可不转 23，1,418+23=1,441）。
不影响本道判据，仅供协调者核对表述。

## 没做的

- 存量真转（任务书 §六点名不碰，且必须与 UI 道配对上线）
- 宋史那 24 处（md 23 ＋ json 1）跨段/跨字段夹注的人工判读——留给整理《宋史·藝文志》
  内容的人（同任务书 §六「不入库」）
- `〔…〕` 处置未变（仍按 09-10 裁决默认不转），未新增分类档

---
道：E-记法转换
