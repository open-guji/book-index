08 档「（（…))」全库 1,423 处的口径，与本道实测的 1,441 对不上，请核。

## 出入

任务书-E-记法转换.md §三·第二件写「协调者 2026-09-14 实测：`（（…))` 是第四种夹注记号，
全库 1,423 处，全部在《宋史·藝文志》`d59f2gdp696q` 的 md 侧」。

本道用新写的转换器在同一份 md 语料上跑闸，实测：

- 原始子串数：`（（` 与 `））` 各 **1,441** 次（两侧计数一致，非漏抓）
- 其中**同一行内可安全配对**（栈式配对，起止都在同一行）：**1,418** 处
- 判两可不转的**跨段**（旧式排法：夹注跨多段时每段重开 `（（`，只在末段收尾 `））`）：**23** 处
- 1,418 + 23 = 1,441，账能对平，只是不等于 1,423

json 侧另有 1 处（`juan/012.json` 的《寧宗實錄》/《通鑒補遺》两个相邻 section），
也是同一类跨段（这次是跨 JSON 字段），不计入上面 md 的 1,441。

## 我的判断

推测 1,423 是扫描时的粗计数口径（大概率是别的统计方式，比如去重或抓取方式不同），
不影响本道判据——任务书 §一·3 要求的「463 处」是汉志的，与这处数无关，
本道也没有因为这处出入而改动任何转换逻辑。仅供协调者核对 08 档表述，若需要我可以
补一份逐处比对脚本，但目前判断价值不大（两边算法不同，很难反推出协调者当时具体用的是哪种数法）。

详情与逐处位置见 `overview` 仓
`项目进展/古籍索引网站/目录录入/二十五史艺文志考补/reports/20260914-记法转换/README.md` §三。

---
道：E-记法转换
