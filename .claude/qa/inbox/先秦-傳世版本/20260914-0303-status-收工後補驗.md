# status：收工後補驗資源，一項驗不了，如實報備

- 道：`先秦-傳世版本`　日期：2026-09-14（done 單之後追記）

## 补驗了什麼

done 單交出後，覆查發現**新建 5 個 Book 的資源尚未逐條驗過**（此前只驗了
維基文庫 11 個 URL 與維基共享 Category 頁）。今補驗，結果分兩類。

## 一、驗過且合格（2 條）

| Book | 資源 | 結果 |
|---|---|---|
| `98ji05um83` 楚辭閔齊伋本 | 維基共享 File:NCL-09285 楚辭.pdf | HTTP 200，115,581 B，File 頁著錄與本 Book 之 edition／current_location／measure_info 逐項對得上 |
| `98ji05um84` 外經微言 | 維基文庫《黃帝外經》 | HTTP 200，284,721 B |

## 二、⚠️ 驗不了（3 條）——識典古籍，本環境無法覆核內容

`98ji05um80`／`98ji05um81`／`98ji05um82` 所掛識典 URL（SBCK066／SK1383／SBCK111）
**HTTP 200 但內容無從證實**：

- 識典是**純客戶端渲染的 React SPA**：curl 取回的 HTML **無 `<title>`**、
  `meta description` 為空、**不含書名**，且三個不同書號的頁面大小幾乎相同
  （244,223／244,217／244,223 位元組）——**HTTP 200 在這裡完全不是證據**
  （正是 `validate-resources`〈三類障礙〉之第二類）
- 帶 `/zh/` 前綴的規範 URL（HTML 中自帶）同樣是空殼，222 KB 亦不含書名
- `/api/book/<id>`、`/api/v1/book/<id>` 諸端點皆落入 catch-all 路由，返回同一空殼；
  `api.shidianguji.com` 走代理 502
- 改用環境內建 Chromium＋Playwright 渲染（已裝 playwright、已配
  `proxy={"server": $HTTPS_PROXY}`、README 稱瀏覽器 NSS store 已信任 CA），
  `wait_until` 用 networkidle 與 domcontentloaded 皆一律
  **`net::ERR_CONNECTION_RESET`**——而 curl 同址卻通。係本環境對瀏覽器出網之限制，
  非站點或資料之病。

**可信度之據**（為什麼仍掛）：

1. 該 URL 與其 metadata **是庫內既有資料**，本道只從 Work 層複製到 Book 層，**非新引入**；
2. metadata 帶 `total_page`／`paragraph_count`／`version`／`check_type` 等精確欄位
   （如 SBCK066 作 total_page=24、paragraph_count=20、version=v10、check_type=精校），
   當是當初自識典 API 抓取，不可能是編造；
3. 本 Book 之 `edition` **直接取自 `metadata.edition`**，未作任何推斷。

已落 3 條 `verdict=open`，rule `V-識典資源本環境無法驗內容`。
**建議**：識典資源之內容覆核須在本地環境用 `validate-resources` skill 補做——
這一類在全庫極多（`shidianguji-*` 系列），值得一並排期，不只本道這 3 條。

## 三、給後手的一條環境事實

**本環境驗 SPA 型資源這條路是斷的**：curl 拿空殼，Chromium 出網被 reset。
凡 tier 3 的識典、以及其他客戶端渲染的站點，雲端道都只能驗到「URL 可達」為止，
**內容覆核必須落到本地**。這一條建議寫進 `validate-resources` skill 的環境前提。
