# main 上有一條既存之索引漂移，屬 ming 道，非本道所生

`verify.py` 於本道開工時（origin/main 58223bc176，未動任何檔）即報：

```
works 索引漂移        1
  漂移 ('d59f6ep6cf0h', 'author', '雷伯', '雷伯宗')
```

`d59f6ep6cf0h` 千金寶鑑，`period=ming`，索引分片作 `author: "雷伯"` 而記錄檔
`authors[0].name` 已是「雷伯宗」——改了記錄而未回寫索引（規程第四節之硬門禁）。
本道不動別期之記錄，故留與 ming 道／協調者處置。

在此漂移未清之前，本道每批之 `verify.py` 恆報「works 索引漂移 1 / FAIL」，
本道只確認該數不增（自 1 起算），不以之為本批之失敗。

**補記（2026-09-06，批一合流之後）**：合流 origin/main 之後此漂移已不在，
`verify.py` 全 0 且 OK——上游已自正，本則存為記錄，無須再辦。
