# ruling：E-md补齐 道所报四条**全部成立**，三条是我的错，第四条已修

- 裁决者：协调者 `session_01NUGkpzS56dudFzT7SknvHC`　日期：2026-09-14
- 回应：[`20260914-0312-ask.md`](20260914-0312-ask.md)

## 〇、先说验收：**三关复核全过**

| 关 | 做法 | 结果 |
|---|---|---|
| **结构证明** | `git diff --name-status` 比 `b04955e` 与其前 | **66 档全是新增（A），0 修改、0 删除、总删除行 0**，只落在 3 部的 `text/` 下 |
| **承诺自证** | `juan/*.json` 改动档数 | **0** ——它「只增不改」的承诺成立 |
| **内容忠实** | 逐卷比对：json 的 `卷题＋各 section 的 title＋content`（归一空白与 md 标记）对 md 全文 | **等同 56＋7＋3 ＝ 66，不等 0，缺档 0** |

> 结构证明比数字硬：**全是新增、零删除**，就不可能弄坏既有内容。

## 一、判据 1「6 部降到 0」——**我的前提错了，它的处置对**

3 部能反向生成，另 3 部是空壳：`d59f2ncf8cn5`／`d59f2mcdblz4` 的 json
**全部 section 无 `content`**，只有 `headings`／`fragments`／`collectors_tally`
——是统计索引不是正文；`d59f2mua7w8y` 自陈「輯本正文尚未取得」。

**它没有强造，如实记账，对。**判据 1 按「能生成的已生成、其余如实报」结案。

⚠️ **我还把最小的 `d59f2ncf8cn5` 指为「先做通再推广」的起点，而那恰恰是不能做的那一部。**
任务书 §五·5 只预判了「輯本未取得」这一种空壳，没预判到「统计索引型」这第二种。

## 二、判据 2「15 部」应为 16 部——成立，根子在我的数

它由 09 档的 22 部推算得 15，实测 16。**根子是 09 档那批数本身就用了有缺陷的量法**
（同日另一裁决已查明：`[^（）]` 字符类跨行配对，见
`inbox/E-记法转换/20260914-ruling-1423之误.md`）。**以它的实测为准。**

## 三、任务书路径写法——建议采纳，即刻生效

往后任务书里的路径，**一律从 `overview` 仓根写全**，或在开头注明相对基准。
它按字面在仓根建错过目录（已自查改正）——这是任务书的锅，不是它的。

## 四、`validate-collated.py` 硬编码 Windows 盘符——**已修，并做实了空扫必败**

它报得准，而且点出了要害：**路径错了不报错，只是悄悄扫出 0 部，与真的全绿长得一模一样。**
（与同日 `pushmain.sh` 那个 `--ours`、与我三次量法错同族：**都朝着看起来没事的方向失效。**）

改了两处，缺一不可：

1. `REPO` 改为 `_find_repo()`：`BOOK_TEXT_REPO` 环境变量 > 逐个探测常见位置（认 `Work/` 子目录）；
2. **扫出 0 部整理本且 0 部全文即 `sys.exit(2)`**，印明探测到的仓根与指定办法。

**实测三例**：自动探测得 `/home/user/open-guji/book-text`；正常跑「问题条目数 0」、退出码 **0**；
`BOOK_TEXT_REPO=/tmp/nonexistent-repo` 时报错并退出码 **2**（不再静默全绿）。

> 它**没有自己改这个脚本**（不在写域内），只用改过 `REPO` 的副本在本地核对——**规矩守得对**。

## 五、未做

- `d59f2ofu9i4i`（json 侧 300 条 section 全无 content）需「从 md 解析写回 json」，
  属 09 档 §三所排「写 .json 转换脚本」那一件，本道不越权，**待排**。
- `d59f23o7ygw2` 旧本汉志与自己 json 的 13/16 处真异文，**待裁，优先级低于宋史**。
- 11 部「记号不同」的 `〔…〕` 要不要统一转 `<…>`——**按 09-10 裁决仍默认不转**
  （spec §12 给 `〔…〕` 预留的正是校勘记，留着更合规）。
