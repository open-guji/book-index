E-md补齐 道任务书四条判据全部办竣，两仓已推 main。

## 做了什么

1. **6 部无 md → 3 部生成、3 部空壳如实报**（详情见 `ask` 单第 1 条）
   - 生成：`d59f2htm01du` 直齋書錄解題（3415 条）、`d59f2mp0quip` 漢書藝文志拾補（309 条）、
     `d59f2o2lx91e` 漢書藝文志疏證（642 条），逐条与 json 对账（条目数相等、正文逐字等同），
     脚本 `verify_md.py`，三部全过
   - 未生成：`d59f2ncf8cn5`、`d59f2mcdblz4`（json 无实质 content，纯统计索引）、
     `d59f2mua7w8y`（`source_note` 自陈"輯本正文尚未取得"），均如实记入报告，未强造
2. **16 部记号处数对不上，逐部定性**（任务书写"15 部"，实测 16 部，见 `ask` 单第 2 条）：
   - 11 部"记号不同、内容不缺"（md 用〔〕给按语划界，json 同一文字去括号直接拼入 content）
   - 1 部（`d59f2ofu9i4i`）**真缺**：json 侧 300 条 section 全部无 content，只有骨架
   - 1 部（`d59f23o7ygw2`，book-text 现行旧本汉志）两侧互有少量真异文，规模很小
   - 3 部计数口径误判，无需处理（`d59f2mel8d1d` 千頃堂图版引用码非夹注、`d59f2nfhf8cg`
     明朝武职选簿"又壹員"标注非夹注、`d59f2ofmgkch` 标题自带半个括号+统计脚本口径不对称）
3. `juan/*.json` **零改动**，`git diff --stat` 已自证（报告 §六）
4. 顺手发现两个坑：`validate-collated.py` 路径写死 Windows 盘符且路径错时**静默扫出 0 文件
   而非报错**（见 `ask` 单第 4 条）；本任务书路径写法与实际仓层级不一致（`ask` 单第 3 条）

## 报告与提交

- 报告：`overview` 仓 `项目进展/古籍索引网站/目录录入/二十五史艺文志考补/reports/20260914-md补齐/报告.md`
  （commit `1fb2afd`，已推 main，含全部数字的量法与命令）
- `book-text`：新增 66 个 `text/*.md` 文件（56+7+3），commit `b04955e`，已推 main
  （先推分支 `claude/E-md补齐-20260914`，fast-forward 合入 main）
- `validate-collated` skill 校验：64 部整理本全绿，问题条目数 0

## 未做 / 留给下一道

- `d59f2ofu9i4i` 的 json 真缺内容需要"从 md 解析写回 json"，是 09 档 §三 排的"写 .json 转换脚本"
  那一件的活，本道不越权（写域不含 `juan/*.json`）
- `d59f23o7ygw2`（book-text 现行汉志旧本）与自己 json 的少量真异文（13/16 处，很小）未处理，
  列入待裁但优先级远低于宋史
- 11 部"记号不同"里的〔〕要不要统一转成新格式夹注 `<jz>`，是 E-记法转换 道或后续裁决的事，
  本道只坐实了"这 11 部是记号问题不是内容问题"这一结论

## 收工时状态

已写入任务书末尾。
